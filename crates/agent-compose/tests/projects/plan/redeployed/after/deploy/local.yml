# The same target, with one placement's membership widened, a second one added,
# and the hub given a public base.
version: "0.1"

hub:
  join_token: ${MESH_JOIN_TOKEN}
  public_url: "http://localhost:8080"

placements:
  reviewers:
    members: [agent.reviewer]
  writers:
    members: [agent.writer]
    description: Its own machine, even locally.
