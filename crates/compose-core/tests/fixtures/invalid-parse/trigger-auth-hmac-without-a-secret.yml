# rule: an inbound `hmac:` scheme declares the key it verifies signatures with (grammar 13.3, PRD resolved q32)
# code: missing-key
# message: missing required key `secret` in the `hmac` of the `auth` of trigger `intake`
# at: 10:13
triggers:
  intake:
    type: http
    flow: flow.support
    auth:
      hmac: {}
