# rule: `version:` must be one of the build's SUPPORTED_SPEC_VERSIONS (grammar 1.3, PRD 9.5)
# code: unsupported-version
# message: `9.9` is not a spec version this compiler supports
# at: 6:10
# help: set `version:` to one this build supports (`0.1`), and bring forward any syntax the older version used — changing this field alone is not a migration
version: "9.9"
