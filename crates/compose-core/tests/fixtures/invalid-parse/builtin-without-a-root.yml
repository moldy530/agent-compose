# rule: every built-in takes a mandatory `root:` (grammar 5.5, D123)
# code: missing-key
# message: missing required key `root` in `builtin.read_file` in agent definition `agent.reviewer`
# at: 9:26
agent.reviewer:
  model: model.smart
  prompt: Review the draft.
  tools:
    - builtin.read_file: {}
  output:
    verdict: { enum: [approve, revise] }
