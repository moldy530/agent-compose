# rule: a built-in is attached as a mapping carrying its bounds, because the
# rule: bounds are not optional (grammar 5.5, D123)
# code: invalid-reference
# message: `builtin.bash` is a built-in tool, not a definition to reference
# at: 11:7
# help: a built-in is attached as a mapping carrying its bounds: `- builtin.bash: { root: <directory>, timeout: 30s }` (grammar 5.5)
agent.reviewer:
  model: model.smart
  prompt: Review the draft.
  tools:
    - builtin.bash
  output:
    verdict: { enum: [approve, revise] }
