# rule: a node carries exactly one kind key (grammar 7.1)
# code: missing-key
# message: node `write` declares no kind key: a node carries exactly one of `agent`, `exec`, `http`, `function`, `flow`, `map`, `human`, `store`
# at: 11:7
# help: the kind key selects what the node does and what config it takes
flow.demo:
  outputs:
    draft: { type: string }
  nodes:
    write:
      input: { goal: "input.goal" }
  edges:
    - { from: start, to: write }
    - { from: write, to: end }
