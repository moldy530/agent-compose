# rule: a placement member is an `agent.*` or `tool.*` address (grammar 14.1, Decision D129)
# code: invalid-reference
# message: expected an `agent.*` or `tool.*` reference for a placement member, found `store.docs`
# at: 12:15
version: "0.1"

hub:
  join_token: ${MESH_JOIN_TOKEN}

placements:
  mac:
    members: [store.docs]
