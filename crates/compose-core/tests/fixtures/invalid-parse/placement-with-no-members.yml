# rule: a placement's `members:` list is non-empty (grammar 14.1)
# code: invalid-value
# message: the `members` of placement `mac` names no component
# at: 13:14
# help: a placement is the set of components a worker claiming its name runs, so one with no members is a claim nothing is ever dispatched under: name the components it runs, or drop the placement (grammar 14.1, PRD resolved q38)
version: "0.1"

hub:
  join_token: ${MESH_JOIN_TOKEN}

placements:
  mac:
    members: []
