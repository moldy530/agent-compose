# rule: backend connection strings are env-ref values, never literals (grammar 14.3, 4.3)
# code: invalid-env-ref
# message: `url` in backend alias `docs_db` takes an environment reference, found the literal `https://chroma.internal:8000`
# at: 12:12
# help: the spec never contains credentials or connection strings: write `${NAME}`
version: "0.1"

storage_backends:
  aliases:
    docs_db:
      provider: chroma
      url: https://chroma.internal:8000
