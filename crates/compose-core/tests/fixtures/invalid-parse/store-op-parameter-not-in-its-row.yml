# rule: a store op's parameter list is exact (grammar 11.4, Decision D34)
# code: invalid-value
# message: `top_k` is not a parameter of the `get` op
# at: 14:7
# help: `get` takes `key`
flow.demo:
  outputs:
    draft: { type: string }
  nodes:
    load:
      store: store.memory
      op: get
      key: "execution.session_key"
      top_k: 3
  edges:
    - { from: start, to: load }
    - { from: load, to: end }
