# rule: an inbound `auth:` block declares exactly one scheme (grammar 13.3, PRD resolved q32)
# code: missing-key
# message: the `auth` of trigger `intake` declares no scheme: an inbound `auth` carries exactly one of `bearer`, `hmac`
# at: 10:5
# help: `bearer` compares a static secret against a named header, `hmac` verifies a signature over the raw request body — drop `auth:` altogether to leave the route open (grammar 13.3, PRD resolved q32)
triggers:
  intake:
    type: http
    flow: flow.support
    auth: {}
