# rule: `callback_allow:` is a key of `http` triggers alone (grammar 13.3, PRD resolved q33)
# code: unknown-key
# message: unknown key `callback_allow` in trigger `ingest`
# at: 10:5
triggers:
  ingest:
    type: event
    flow: flow.support
    source: bug_reports
    callback_allow:
      - "https://hooks.example.com/*"
