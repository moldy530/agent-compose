# rule: a placement carries `members:` and `description:` and nothing else (grammar 14.1)
# code: unknown-key
# message: unknown key `runtime` in placement `mac`
# at: 13:5
version: "0.1"

hub:
  join_token: ${MESH_JOIN_TOKEN}

placements:
  mac:
    members: [agent.signer]
    runtime: isolated
