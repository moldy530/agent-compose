# rule: each provider kind fixes its required keys (grammar 12.1, Decision D38)
# code: missing-key
# message: missing required key `base_url` in provider definition `provider.local`: `openai_compatible` providers declare it
# at: 11:3
# note: an `openai_compatible` provider rather than an `anthropic` one, because
# note: the two kinds with a default endpoint require nothing outright: their
# note: `api_key:` is conditional on `base_url:`, which is a separate rule of the
# note: same pass and reports `missing-credential` instead — Decision D120,
# note: `tests/fixtures/invalid-parse/provider-reaches-the-vendor-with-no-key.yml`.
provider.local:
  kind: openai_compatible
