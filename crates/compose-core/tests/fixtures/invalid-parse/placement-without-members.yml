# rule: a placement declares `members:` (grammar 14.1)
# code: missing-key
# message: missing required key `members` in placement `mac`
# at: 12:5
version: "0.1"

hub:
  join_token: ${MESH_JOIN_TOKEN}

placements:
  mac:
    description: The machine with the signing keys.
