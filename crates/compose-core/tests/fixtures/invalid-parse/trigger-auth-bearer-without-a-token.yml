# rule: an inbound `bearer:` scheme declares the token it compares against (grammar 13.3, PRD resolved q32)
# code: missing-key
# message: missing required key `token` in the `bearer` of the `auth` of trigger `intake`
# at: 10:15
triggers:
  intake:
    type: http
    flow: flow.support
    auth:
      bearer: {}
