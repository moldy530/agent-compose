# rule: `builtin.bash` takes a mandatory `timeout:` beside its `root:`; a
# rule: default would be a bound nobody wrote and nobody read (grammar 5.5, D123)
# code: missing-key
# message: missing required key `timeout` in `builtin.bash` in agent definition `agent.reviewer`
# at: 10:21
agent.reviewer:
  model: model.smart
  prompt: Review the draft.
  tools:
    - builtin.bash: { root: "./work" }
  output:
    verdict: { enum: [approve, revise] }
