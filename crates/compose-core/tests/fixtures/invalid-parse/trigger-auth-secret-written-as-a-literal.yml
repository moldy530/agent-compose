# rule: an `auth:` secret takes an environment reference, never a literal (grammar 4.3, 13.3, Decision D41)
# code: invalid-env-ref
# message: `secret` takes an environment reference, found the literal `s3cr3t`
# at: 12:17
# help: the spec never contains credentials or connection strings: write `${NAME}`
triggers:
  intake:
    type: http
    flow: flow.support
    auth:
      hmac:
        secret: s3cr3t
