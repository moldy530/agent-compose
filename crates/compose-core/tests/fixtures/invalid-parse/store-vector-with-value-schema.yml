# rule: `value_schema` is illegal on a vector store (grammar 11.1)
# code: conflicting-keys
# message: `value_schema` is not legal on a `vector` store
# at: 11:3
store.docs:
  kind: vector
  scope: global
  embed:
    model: text-embedding-3-small
    provider: provider.openai
  value_schema:
    text: { type: string }
