# rule: an outbound `hmac:` scheme declares the key it signs deliveries with (grammar 13.3, PRD resolved q33)
# code: missing-key
# message: missing required key `secret` in the `hmac` of the `callback_auth` of trigger `intake`
# at: 11:13
triggers:
  intake:
    type: http
    flow: flow.support
    callback: "payload.body.callback_url"
    callback_auth:
      hmac: {}
    callback_allow:
      - "https://hooks.example.com/*"
