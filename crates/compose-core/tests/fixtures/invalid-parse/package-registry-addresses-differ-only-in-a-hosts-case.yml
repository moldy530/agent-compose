# rule: a `package_registry` address is the one npm derives, so two hosts spelled in different cases are one address (grammar 14.6)
# code: conflicting-registry-credential
# message: `@corp` and `package_registry` authenticate to `//npm.internal.example/repository/` with two different variables
# at: 17:14
# help: an `.npmrc` credential is keyed by address rather than by scope — the registry's authority and its whole path, so one written with a trailing `/` and one written without it are the same address — and two entries sharing one key write one `_authToken` line that an installer resolves last-one-wins: give each its own path on the mirror, or give both the same variable (grammar 14.6, PRD resolved q59)
version: "0.1"

# npm derives its lookup key from the request URI through a WHATWG `URL`, which
# lowercases a host — so these two addresses are one address, and an emitted
# `.npmrc` would carry one `_authToken` line twice with two variables in it.
package_registry:
  url: "https://NPM.Internal.Example/repository/"
  token: ${NPM_MIRROR_TOKEN}
  scopes:
    "@corp":
      url: "https://npm.internal.example/repository/"
      token: ${NPM_CORP_TOKEN}
