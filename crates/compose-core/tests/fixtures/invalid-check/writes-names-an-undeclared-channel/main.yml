# rule: a `writes:` value is a declared channel (grammar 10.3)
# code: undefined-channel
# message: `drafts` is a `writes:` destination that `state:` does not declare
# at: main.yml:28:25
# help: did you mean `draft`?
version: "0.1"
provider.p:
  kind: anthropic
  api_key: ${K}
model.m:
  provider: provider.p
  id: some-model
state:
  draft: { type: string, default: "" }
agent.a:
  model: model.m
  prompt: Do the thing.
  input:
    text: { type: string }
  output:
    result: { type: string }
flow.f:
  outputs: {}
  nodes:
    n:
      agent: agent.a
      input: { text: "'x'" }
      writes: { result: drafts }
  edges:
    - { from: start, to: n }
    - { from: n, to: end }
