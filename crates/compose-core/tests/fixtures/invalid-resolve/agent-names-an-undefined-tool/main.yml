# rule: an agent's `tools:` entries resolve (grammar 2.3)
# code: undefined-reference
# message: `tool.web_search` is not defined in this composition
# at: main.yml:16:11
# help: the composition defines no `tool.*` or `flow.*`: a definition is part of it only when the entrypoint imports the file that declares it (grammar 1.4)
version: "0.1"
provider.p:
  kind: anthropic
  api_key: ${KEY}
model.m:
  provider: provider.p
  id: some-model
agent.a:
  model: model.m
  prompt: Do the thing.
  tools: [tool.web_search]
  output:
    text: { type: string }
