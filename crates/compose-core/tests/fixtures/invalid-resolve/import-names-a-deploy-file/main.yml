# rule: `deploy/*.yml` may not appear in `imports:` (grammar 1.4)
# code: invalid-import-path
# message: `deploy/staging.yml` is a deploy file and may not be imported
# at: main.yml:8:5
# help: deploy files are selected with `--target <name>`, which loads `deploy/<name>.yml`; only that layer forks per environment (grammar 14)
version: "0.1"
imports:
  - deploy/staging.yml
