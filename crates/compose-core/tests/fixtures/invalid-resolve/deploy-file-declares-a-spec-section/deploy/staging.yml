# `imports:` makes the parser read this as a spec file, so its own "a deploy
# file must declare `version:`" rule is not the one in play — and the resolver,
# which knows this file was selected by `--target staging`, refuses it for the
# section rather than telling it that it is the entrypoint (grammar 1.3, 1.5).
imports:
  - agents/reviewer.yml
