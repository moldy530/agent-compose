# rule: a placement `members:` entry resolves in the composition (grammar 14.1)
# code: undefined-reference
# message: `tool.repo_grap` is not defined in this composition
# at: deploy/local.yml:8:15
# help: did you mean `tool.repo_grep`?
version: "0.1"
tool.repo_grep:
  description: Search the repository for a pattern.
  input:
    pattern: { type: string }
  output:
    matches: { type: string }
  exec:
    command: repo-grep
flow.f:
  outputs: {}
  nodes:
    run: { exec: { command: "true" } }
  edges:
    - { from: start, to: run }
    - { from: run, to: end }
