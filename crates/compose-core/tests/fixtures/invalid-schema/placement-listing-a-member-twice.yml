# rule: a placement names each member once (grammar 14.1)
# at: /placements/mac/members
# via: /$defs/placement/properties/members/uniqueItems
version: "0.1"
hub:
  join_token: ${MESH_JOIN_TOKEN}
placements:
  mac:
    members: [agent.signer, agent.signer]
