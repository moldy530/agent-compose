# rule: a model definition is a direct binding XOR an ordered route, never both (grammar 12.2 / PRD 5.9)
# at: /model.default
# via: /$defs/modelRouteForm/additionalProperties
model.default:
  provider: provider.anthropic
  id: claude-sonnet-4-6
  route: [model.smart, model.fast]
