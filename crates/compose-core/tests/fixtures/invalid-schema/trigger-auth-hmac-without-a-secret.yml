# rule: an inbound `hmac:` scheme declares the key it verifies signatures with (grammar 13.3 / PRD resolved q32)
# at: /triggers/intake/auth/hmac
# via: /$defs/inboundHmac/required
triggers:
  intake:
    type: http
    flow: flow.support
    auth:
      hmac: {}
