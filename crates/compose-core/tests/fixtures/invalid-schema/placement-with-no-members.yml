# rule: a placement's members list is non-empty (grammar 14.1)
# at: /placements/mac/members
# via: /$defs/placement/properties/members/minItems
version: "0.1"
hub:
  join_token: ${MESH_JOIN_TOKEN}
placements:
  mac:
    members: []
