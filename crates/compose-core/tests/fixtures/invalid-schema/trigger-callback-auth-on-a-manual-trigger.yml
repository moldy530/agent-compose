# rule: `callback_auth:` is a key of `http` triggers alone (grammar 13.3 / PRD resolved q33)
# at: /triggers/cli
# via: /$defs/manualTrigger/additionalProperties
triggers:
  cli:
    type: manual
    flow: flow.support
    callback_auth:
      hmac:
        secret: ${CALLBACK_SECRET}
