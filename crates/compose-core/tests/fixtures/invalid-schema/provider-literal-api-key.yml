# rule: credentials take ${ENV_VAR} references only, never literals (grammar 4.3 / PRD 5.9)
# at: /provider.anthropic/api_key
# via: /$defs/envRef/pattern
provider.anthropic:
  kind: anthropic
  api_key: sk-ant-not-a-real-key
