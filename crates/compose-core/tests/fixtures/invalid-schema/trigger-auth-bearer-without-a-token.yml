# rule: an inbound `bearer:` scheme declares the token it compares against (grammar 13.3 / PRD resolved q32)
# at: /triggers/intake/auth/bearer
# via: /$defs/bearerAuth/required
triggers:
  intake:
    type: http
    flow: flow.support
    auth:
      bearer: {}
