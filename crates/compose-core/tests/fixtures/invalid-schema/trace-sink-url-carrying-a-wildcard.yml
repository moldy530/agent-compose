# rule: trace_sink.url is this deployment's own collector rather than a pattern, so it carries no wildcard (grammar 14.5)
# at: /trace_sink/url
# via: /$defs/traceSink/properties/url/allOf/1/pattern
version: "0.1"
trace_sink:
  url: "https://*.collector.example/v1/traces"
