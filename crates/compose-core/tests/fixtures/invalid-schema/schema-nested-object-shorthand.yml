# rule: nested objects are explicit (`type: object` + `properties`); a bare mapping is not a shorthand (grammar 3.4 / PRD 5.2)
agent.reviewer:
  model: model.smart
  prompt: Review the draft.
  output:
    author:
      name: { type: string }
      email: { type: string }
