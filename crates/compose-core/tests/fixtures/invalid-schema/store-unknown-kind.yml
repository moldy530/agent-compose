# rule: store kinds are exactly kv | vector | blob - relational/SQL is deliberately excluded (grammar 11.1 / PRD 5.8)
# at: /store.tickets/kind
# via: /$defs/storeDef/properties/kind/enum
store.tickets:
  kind: sql
  scope: global
  value_schema:
    id: { type: string }
