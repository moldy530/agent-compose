# rule: a placement's runtime is `isolated` or `colocated` (grammar 14.1 / PRD 5.10)
# at: /placements/agent.researcher/runtime
# via: /$defs/placement/properties/runtime/enum
version: "0.1"
placements:
  agent.researcher:
    runtime: serverless
