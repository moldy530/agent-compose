# rule: spec files and deploy files are disjoint - only the deploy layer forks per target (grammar 1.2 / PRD 5.8)
# at: /agent.fixer
# via: /$defs/deployFileOnly/patternProperties/^(agent|tool|flow|store|provider|model)\.
version: "0.1"
hub:
  join_token: ${MESH_JOIN_TOKEN}
placements:
  mac:
    members: [agent.fixer]
agent.fixer:
  model: model.fast
  prompt: Fix the finding.
  output:
    patch: { type: string }
