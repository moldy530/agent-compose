# rule: a file declaring `imports:` is the entrypoint and MUST declare `version:` (grammar 1.3 / PRD 9.5)
# at: <root>
# via: /dependentRequired
imports:
  - agents/reviewer.yml
  - flows/review_loop.yml
