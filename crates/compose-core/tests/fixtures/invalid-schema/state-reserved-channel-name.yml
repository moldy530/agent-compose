# rule: reserved names (CEL roots and the implicit conversation-history channel) may not be state channels (grammar 2.5 / PRD 5.7)
state:
  messages:
    type: array
    items: { type: string }
    reduce: append
