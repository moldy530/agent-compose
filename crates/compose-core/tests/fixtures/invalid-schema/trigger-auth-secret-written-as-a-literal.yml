# rule: an `auth:` secret takes the env-ref value form; a secret never lives in the spec text (grammar 4.3, 13.3, D41 / PRD resolved q32)
# at: /triggers/intake/auth/hmac/secret
# via: /$defs/envRef/pattern
triggers:
  intake:
    type: http
    flow: flow.support
    auth:
      hmac:
        secret: s3cr3t
