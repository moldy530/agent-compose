# rule: `callback:` is async-only — a synchronous response already returns the outputs (grammar 13.3)
# at: /triggers/on_request
triggers:
  on_request:
    type: http
    flow: flow.review_loop
    path: /review
    respond: sync
    timeout: 30s
    callback: "payload.body.callback_url"
