// This file was generated by agent-compose 0.0.0-dev from `main.yml` (target `mesh`).
//
// It is a build artifact, not source: `agent-compose build` overwrites it,
// and `agent-compose build --check` fails when it has been edited. The spec
// is the single source of truth (PRD 5.12); to own this code instead, copy
// the whole directory out and stop regenerating it.
//
// The compiled graph (PRD 5.3, 5.4, grammar 7, 8, 9).
//
// One LangGraph node per flow node, driven by the descriptor beside it: how its
// input is built (grammar 8.0), the activity it runs (grammar 8), where its
// output goes (grammar 10.3), and its outgoing edges in declaration order
// (grammar 7.3). Every node answers a `Command`, so the branch it takes and the
// state it writes — including the counter a bounded edge spends (grammar 7.4) —
// land in one write. `./runtime.ts` is what the descriptors drive.

import { END, START, StateGraph } from "@langchain/langgraph";

import * as mesh from "./mesh.ts";
import * as runtime from "./runtime.ts";
import * as stores from "./stores.ts";
import {
  toolStampModule,
} from "./modules.ts";
import {
  agentBrieferOutput,
  agentEscalatorOutput,
  agentSignerOutput,
  flowAbandonedInputs,
  flowBatchInputs,
  flowConversationInputs,
  flowDeadlineInputs,
  flowDeadlineNodeAskOutput,
  flowDeadlineNodeLapseOutput,
  flowDirectInputs,
  flowEscalatedInputs,
  flowEscalationInputs,
  flowEscalationNodeAskOutput,
  flowImpatientInputs,
  flowReleaseInputs,
  flowRetriedInputs,
  flowSignedOffInputs,
  flowSignedOffNodeApproveOutput,
  flowStampedInputs,
  flowWatchedInputs,
  toolNotarizeInput,
  toolNotarizeOutput,
  toolSignInput,
  toolSignOutput,
  toolStampInput,
  toolStampOutput,
} from "./schemas.ts";
import { State } from "./state.ts";
import type { GraphState } from "./state.ts";

// --- Shapes: the declared type an expression reads a value through ---
/**
 * Every declared state channel, so `state.count` is an `int` where the
 * channel says `type: integer` and a `double` where it says `type: number`.
 */
const stateShape: runtime.Shape = {
  properties: {
    "approval": "string",
    "countersignature": "string",
    "signature": "string",
    "signatures": {
      "items": "string"
    },
    "ticket": "string",
  },
};

/** `flow.abandoned` — the `input` root inside it (grammar 7.5). */
const flowAbandonedShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/**
 * `flow.abandoned` node `escalate` — the `escalate.output` root its guards read.
 */
const flowAbandonedNodeEscalateShape: runtime.Shape = {
  "properties": {
    "approval": "string"
  }
};

/** `flow.batch` — the `input` root inside it (grammar 7.5). */
const flowBatchShape: runtime.Shape = {
  "properties": {
    "paths": {
      "items": "string"
    }
  }
};

/** `flow.conversation` — the `input` root inside it (grammar 7.5). */
const flowConversationShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/**
 * `flow.conversation` node `brief` — the `brief.output` root its guards read.
 */
const flowConversationNodeBriefShape: runtime.Shape = {
  "properties": {
    "brief": "string"
  }
};

/** `flow.conversation` node `sign` — the `sign.output` root its guards read. */
const flowConversationNodeSignShape: runtime.Shape = {
  "properties": {
    "signature": "string"
  }
};

/** `flow.deadline` — the `input` root inside it (grammar 7.5). */
const flowDeadlineShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/** `flow.deadline` node `ask` — the `ask.output` root its guards read. */
const flowDeadlineNodeAskShape: runtime.Shape = {
  "properties": {
    "decision": "string"
  }
};

/** `flow.deadline` node `lapse` — the `lapse.output` root its guards read. */
const flowDeadlineNodeLapseShape: runtime.Shape = {
  "properties": {
    "decision": "string"
  }
};

/** `flow.direct` — the `input` root inside it (grammar 7.5). */
const flowDirectShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/** `flow.direct` node `sign` — the `sign.output` root its guards read. */
const flowDirectNodeSignShape: runtime.Shape = {
  "properties": {
    "signature": "string"
  }
};

/** `flow.escalated` — the `input` root inside it (grammar 7.5). */
const flowEscalatedShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/**
 * `flow.escalated` node `escalate` — the `escalate.output` root its guards read.
 */
const flowEscalatedNodeEscalateShape: runtime.Shape = {
  "properties": {
    "approval": "string"
  }
};

/** `flow.escalation` — the `input` root inside it (grammar 7.5). */
const flowEscalationShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/** `flow.escalation` node `ask` — the `ask.output` root its guards read. */
const flowEscalationNodeAskShape: runtime.Shape = {
  "properties": {
    "decision": "string"
  }
};

/** `flow.impatient` — the `input` root inside it (grammar 7.5). */
const flowImpatientShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/**
 * `flow.impatient` node `escalate` — the `escalate.output` root its guards read.
 */
const flowImpatientNodeEscalateShape: runtime.Shape = {
  "properties": {
    "approval": "string"
  }
};

/** `flow.release` — the `input` root inside it (grammar 7.5). */
const flowReleaseShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/** `flow.release` node `sign` — the `sign.output` root its guards read. */
const flowReleaseNodeSignShape: runtime.Shape = {
  "properties": {
    "signature": "string"
  }
};

/** `flow.release` node `stamp` — the `stamp.output` root its guards read. */
const flowReleaseNodeStampShape: runtime.Shape = {
  "properties": {
    "ticket": "string"
  }
};

/** `flow.retried` — the `input` root inside it (grammar 7.5). */
const flowRetriedShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/** `flow.retried` node `sign` — the `sign.output` root its guards read. */
const flowRetriedNodeSignShape: runtime.Shape = {
  "properties": {
    "signature": "string"
  }
};

/** `flow.signed_off` — the `input` root inside it (grammar 7.5). */
const flowSignedOffShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/** `flow.signed_off` node `sign` — the `sign.output` root its guards read. */
const flowSignedOffNodeSignShape: runtime.Shape = {
  "properties": {
    "signature": "string"
  }
};

/**
 * `flow.signed_off` node `approve` — the `approve.output` root its guards read.
 */
const flowSignedOffNodeApproveShape: runtime.Shape = {
  "properties": {
    "decision": "string"
  }
};

/** `flow.stamped` — the `input` root inside it (grammar 7.5). */
const flowStampedShape: runtime.Shape = {
  "properties": {
    "path": "string"
  }
};

/** `flow.stamped` node `stamp` — the `stamp.output` root its guards read. */
const flowStampedNodeStampShape: runtime.Shape = {
  "properties": {
    "stamped": "string"
  }
};

/** `flow.watched` — the `input` root inside it (grammar 7.5). */
const flowWatchedShape: runtime.Shape = {
  "properties": {
    "paths": {
      "items": "string"
    }
  }
};

/**
 * `provider.mock` — an `anthropic` connection (grammar 12.1). Every value is read when a node calls it, never at import: a build carries no credential and `./index.ts` is where their presence is checked (PRD 5.9).
 */
const providerMock: runtime.ProviderBinding = {
  address: "provider.mock",
  kind: "anthropic",
  get apiKey(): string {
    return runtime.environmentValue("MOCK_API_KEY", "provider.mock.api_key");
  },
  get baseUrl(): string {
    return runtime.environmentValue("MOCK_BASE_URL", "provider.mock.base_url");
  },
};

/** `model.smart` — `claude-sonnet-4-6` on `provider.mock` (grammar 12.2). */
const modelSmart: runtime.ModelBinding = {
  address: "model.smart",
  id: "claude-sonnet-4-6",
  provider: providerMock,
  settings: {
    "max_tokens": 4096,
  },
};

/**
 * `tool.notarize` — a subprocess (grammar 6.1). Its arguments are parsed with its own declared `input:` before the implementation sees them, which is the checked signature grammar 8.4 asks for. This is the parse a `function:` node's binding faces, and it is a `ResultMismatch` that fails the node: the arguments are the composition's, checked field-by-field at compile time, so a value constraint they miss at runtime is the graph's own failure and there is nobody to hand it to. A **model's** arguments are refused one level out, at the entry in the agent's `tools:`, where `runtime.parseToolArguments` raises the `ToolCallRefused` the loop hands back (Decision D119). The tool's **result** is parsed with `runtime.parseResult` on both surfaces: a tool answering off-contract is not a call anybody can rephrase.
 */
async function toolNotarize(args: unknown, context: runtime.RunContext): Promise<unknown> {
  const input = runtime.parseResult(toolNotarizeInput, args, "the arguments `tool.notarize` was called with");
  return runtime.parseResult(
    toolNotarizeOutput,
    await runtime.runExec({
      command: [{ env: "OPS_BIN", site: "tool.notarize.exec.command" }, "/echo"],
      args: [
        ["notarized"],
      ],
      env: [],
      expectExit: [0],
      decoding: { envelope: [], decoded: [], raw: "ticket", empty: false },
    }, input, context),
    "the result of `tool.notarize`",
  );
}

/**
 * `tool.sign` — a subprocess (grammar 6.1). Its arguments are parsed with its own declared `input:` before the implementation sees them, which is the checked signature grammar 8.4 asks for. This is the parse a `function:` node's binding faces, and it is a `ResultMismatch` that fails the node: the arguments are the composition's, checked field-by-field at compile time, so a value constraint they miss at runtime is the graph's own failure and there is nobody to hand it to. A **model's** arguments are refused one level out, at the entry in the agent's `tools:`, where `runtime.parseToolArguments` raises the `ToolCallRefused` the loop hands back (Decision D119). The tool's **result** is parsed with `runtime.parseResult` on both surfaces: a tool answering off-contract is not a call anybody can rephrase.
 */
async function toolSign(args: unknown, context: runtime.RunContext): Promise<unknown> {
  const input = runtime.parseResult(toolSignInput, args, "the arguments `tool.sign` was called with");
  return runtime.parseResult(
    toolSignOutput,
    await runtime.runExec({
      command: [{ env: "OPS_BIN", site: "tool.sign.exec.command" }, "/echo"],
      args: [
        ["{\"signature\":\"signed\"}"],
      ],
      env: [
        { name: "KEYCHAIN_PASSWORD", value: [{ env: "KEYCHAIN_PASSWORD", site: "tool.sign.exec.env.KEYCHAIN_PASSWORD" }] },
      ],
      expectExit: [0],
      decoding: { envelope: [], decoded: [], raw: "signature", empty: false },
    }, input, context),
    "the result of `tool.sign`",
  );
}

/**
 * `tool.stamp` — an authored module (grammar 6.1). Its arguments are parsed with its own declared `input:` before the implementation sees them, which is the checked signature grammar 8.4 asks for. This is the parse a `function:` node's binding faces, and it is a `ResultMismatch` that fails the node: the arguments are the composition's, checked field-by-field at compile time, so a value constraint they miss at runtime is the graph's own failure and there is nobody to hand it to. A **model's** arguments are refused one level out, at the entry in the agent's `tools:`, where `runtime.parseToolArguments` raises the `ToolCallRefused` the loop hands back (Decision D119). The tool's **result** is parsed with `runtime.parseResult` on both surfaces: a tool answering off-contract is not a call anybody can rephrase.
 */
async function toolStamp(args: unknown, context: runtime.RunContext): Promise<unknown> {
  const input = runtime.parseResult(toolStampInput, args, "the arguments `tool.stamp` was called with");
  return runtime.parseResult(
    toolStampOutput,
    await runtime.callModule({
      address: "tool.stamp",
      path: "src/tools/stamp.ts",
      env: [
        { name: "STAMP_MARKER", value: [{ env: "STAMP_MARKER", site: "tool.stamp.module.env.STAMP_MARKER" }] },
      ],
    }, toolStampModule, input, context),
    "the result of `tool.stamp`",
  );
}

/**
 * `agent.briefer` — one LLM call with structured output (PRD 5.2, grammar 5). The schema below is the **published** JSON Schema of grammar 3.8's table, which is the column the conformance corpus proves equal to the parse its answer then faces.
 */
const agentBriefer: runtime.AgentBinding = {
  address: "agent.briefer",
  prompt: "Describe the release you are given in one line.\n",
  model: modelSmart,
  output: {
    name: "briefer_output",
    description: "The structured output `agent.briefer` must produce.",
    schema: {
      "additionalProperties": false,
      "properties": {
        "brief": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "brief"
      ],
      "type": "object"
    },
  },
  tools: [],
  maxToolIterations: 8,
};

/**
 * `agent.escalator` — one LLM call with structured output (PRD 5.2, grammar 5). The schema below is the **published** JSON Schema of grammar 3.8's table, which is the column the conformance corpus proves equal to the parse its answer then faces.
 */
const agentEscalator: runtime.AgentBinding = {
  address: "agent.escalator",
  prompt: "Ask a person about the path you are given, and report what they answered.\n",
  model: modelSmart,
  output: {
    name: "escalator_output",
    description: "The structured output `agent.escalator` must produce.",
    schema: {
      "additionalProperties": false,
      "properties": {
        "approval": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "approval"
      ],
      "type": "object"
    },
  },
  tools: [
    {
      name: "escalation",
      address: "flow.escalation",
      description: "Ask a person whether a release may go ahead.",
      schema: {
        "additionalProperties": false,
        "properties": {
          "path": {
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      invoke: (args, context, call) =>
        runtime.callSubflowTool(
          { name: "escalation", binding: flowEscalationBinding, inputs: flowEscalationInputs },
          args,
          context,
          call,
        ),
    },
    {
      name: "deadline",
      address: "flow.deadline",
      description: "Ask a person whether a release may go ahead, before a deadline.",
      schema: {
        "additionalProperties": false,
        "properties": {
          "path": {
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      invoke: (args, context, call) =>
        runtime.callSubflowTool(
          { name: "deadline", binding: flowDeadlineBinding, inputs: flowDeadlineInputs },
          args,
          context,
          call,
        ),
    },
  ],
  maxToolIterations: 8,
};

/**
 * `agent.signer` — one LLM call with structured output (PRD 5.2, grammar 5). The schema below is the **published** JSON Schema of grammar 3.8's table, which is the column the conformance corpus proves equal to the parse its answer then faces.
 */
const agentSigner: runtime.AgentBinding = {
  address: "agent.signer",
  prompt: "Sign the path you are given and report what the signature is.\n",
  model: modelSmart,
  output: {
    name: "signer_output",
    description: "The structured output `agent.signer` must produce.",
    schema: {
      "additionalProperties": false,
      "properties": {
        "signature": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "signature"
      ],
      "type": "object"
    },
  },
  tools: [
    {
      name: "sign",
      address: "tool.sign",
      description: "Sign one path with the machine's keychain.",
      schema: {
        "additionalProperties": false,
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      invoke: (args, context) =>
        toolSign(
          runtime.parseToolArguments(toolSignInput, args, "the arguments `sign` was called with"),
          context,
        ),
    },
  ],
  maxToolIterations: 8,
};

// --- flow.abandoned ---

/** `flow.abandoned` node `escalate` — `agent.escalator` (grammar 8.1). */
const flowAbandonedNodeEscalate: runtime.NodeDescriptor = {
  flow: "flow.abandoned",
  node: "escalate",
  // Grammar 9.3, resolved: `retry` from the node, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    retry: {
      max: 2,
      backoffMs: 1,
      multiplier: 2,
      jitter: true,
    },
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowAbandonedShape, state: stateShape, output: flowAbandonedNodeEscalateShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => {
    const answer = await mesh.dispatchPlaced({
      placement: "mac",
      node: "flow.abandoned.escalate",
      execution: view.run.execution.id,
      itemIndex: view.run.execution.item_index,
      path: runtime.instancePath(view, "escalate"),
      inputs: input,
      history: runtime.historyTurns(view.state["messages"] as unknown[]),
      policy: view.run.policy,
      signal: context.signal,
      stores: context.storeRecords,
    });
    return {
      output: runtime.parseResult(agentEscalatorOutput, answer.output, "the answer of `agent.escalator`"),
      history: answer.history,
      models: answer.models,
      toolDispatches: answer.toolDispatches,
    };
  },
  writes: [
    { field: "approval", channel: "approval", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.abandoned` — its nodes, its `start` edges, and the compiled graph. */
function flowAbandoned() {
  return new StateGraph(State)
    .addNode("escalate", (state: GraphState) => runtime.runNode(flowAbandonedNodeEscalate, state), {
      ends: [END],
    })
    .addEdge(START, "escalate")
    .compile();
}

/**
 * `flow.abandoned`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowAbandonedGraph = flowAbandoned();

/**
 * `flow.abandoned` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowAbandonedBinding: runtime.SubflowBinding = {
  address: "flow.abandoned",
  outputs: ["approval"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowAbandonedGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowAbandonedGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.batch ---

/**
 * `flow.batch` node `fan` — the fan-out it dispatches (grammar 8.6). `over` resolves against `input.paths`, and `path` is what an instance's own bindings call the item.
 */
const flowBatchNodeFanMap: runtime.MapDescriptor = {
  node: "fan",
  as: "path",
  source: {
    path: "input.paths",
    shape: "any",
  },
  maxConcurrency: 12,
  onItemError: "fail",
  routes: [
    {
      target: "agent.signer",
      maxConcurrency: 12,
      detach: false,
      itemShape: "string",
      input: (roots) => ({
        "path": runtime.toJson(runtime.evaluate("path", roots)),
      }),
      run: async (input, context, site) => {
        const answer = await mesh.dispatchPlaced({
          placement: "mac",
          node: "agent.signer",
          execution: site.execution.id,
          itemIndex: site.execution.item_index,
          path: site.path,
          inputs: input,
          signal: context.signal,
          stores: context.storeRecords,
        });
        return {
          output: runtime.parseResult(agentSignerOutput, answer.output, "the answer of `agent.signer`"),
          models: answer.models,
          toolDispatches: answer.toolDispatches,
        };
      },
      writes: [
        { field: "signature", channel: "signatures", reduce: "append" },
      ],
    },
  ],
};

/** `flow.batch` node `fan` — a fan-out (grammar 8.6). */
const flowBatchNodeFan: runtime.NodeDescriptor = {
  flow: "flow.batch",
  node: "fan",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowBatchShape, state: stateShape, output: "any" },
  input: (_roots, view) => runtime.mapPlan(flowBatchNodeFanMap, view),
  run: async (input, context) =>
    runtime.runMap(flowBatchNodeFanMap, input as runtime.MapPlan, context),
  writes: [],
  edges: [
    { to: END },
  ],
};

/** `flow.batch` — its nodes, its `start` edges, and the compiled graph. */
function flowBatch() {
  return new StateGraph(State)
    .addNode("fan", (state: GraphState) => runtime.runNode(flowBatchNodeFan, state), {
      ends: [END],
    })
    .addEdge(START, "fan")
    .compile();
}

/**
 * `flow.batch`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowBatchGraph = flowBatch();

/**
 * `flow.batch` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowBatchBinding: runtime.SubflowBinding = {
  address: "flow.batch",
  outputs: ["signatures"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowBatchGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowBatchGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.conversation ---

/** `flow.conversation` node `brief` — `agent.briefer` (grammar 8.1). */
const flowConversationNodeBrief: runtime.NodeDescriptor = {
  flow: "flow.conversation",
  node: "brief",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowConversationShape, state: stateShape, output: flowConversationNodeBriefShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => {
    const answer = await runtime.callAgent(
      agentBriefer,
      input,
      runtime.historyTurns(view.state["messages"] as unknown[]),
      context,
      { path: runtime.instancePath(view, "brief"), policy: view.run.policy },
    );
    return {
      output: runtime.parseResult(agentBrieferOutput, answer.output, "the answer of `agent.briefer`"),
      history: answer.history,
      models: answer.models,
      toolDispatches: answer.toolDispatches,
    };
  },
  writes: [],
  edges: [
    { to: "sign" },
  ],
};

/** `flow.conversation` node `sign` — `agent.signer` (grammar 8.1). */
const flowConversationNodeSign: runtime.NodeDescriptor = {
  flow: "flow.conversation",
  node: "sign",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowConversationShape, state: stateShape, output: flowConversationNodeSignShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => {
    const answer = await mesh.dispatchPlaced({
      placement: "mac",
      node: "flow.conversation.sign",
      execution: view.run.execution.id,
      itemIndex: view.run.execution.item_index,
      path: runtime.instancePath(view, "sign"),
      inputs: input,
      history: runtime.historyTurns(view.state["messages"] as unknown[]),
      policy: view.run.policy,
      signal: context.signal,
      stores: context.storeRecords,
    });
    return {
      output: runtime.parseResult(agentSignerOutput, answer.output, "the answer of `agent.signer`"),
      history: answer.history,
      models: answer.models,
      toolDispatches: answer.toolDispatches,
    };
  },
  writes: [
    { field: "signature", channel: "signature", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/**
 * `flow.conversation` — its nodes, its `start` edges, and the compiled graph.
 */
function flowConversation() {
  return new StateGraph(State)
    .addNode("brief", (state: GraphState) => runtime.runNode(flowConversationNodeBrief, state), {
      ends: ["sign"],
    })
    .addNode("sign", (state: GraphState) => runtime.runNode(flowConversationNodeSign, state), {
      ends: [END],
    })
    .addEdge(START, "brief")
    .compile();
}

/**
 * `flow.conversation`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowConversationGraph = flowConversation();

/**
 * `flow.conversation` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowConversationBinding: runtime.SubflowBinding = {
  address: "flow.conversation",
  outputs: ["signature"],
  recursionLimit: 27,
  stream: (initial, options) =>
    flowConversationGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowConversationGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.deadline ---

/**
 * `flow.deadline` node `ask` — the pause it holds: what the human is shown, and what an answer has to fit (grammar 8.7, PRD 5.11).
 */
const flowDeadlineNodeAskHuman: runtime.HumanDescriptor = {
  flow: "flow.deadline",
  node: "ask",
  timeoutMs: 2000,
  onTimeout: "lapse",
  schema: {
    "additionalProperties": false,
    "properties": {
      "decision": {
        "enum": [
          "approve",
          "reject"
        ],
        "type": "string"
      }
    },
    "required": [
      "decision"
    ],
    "type": "object"
  },
  parse: (payload) => runtime.parseResult(flowDeadlineNodeAskOutput, payload, "the answer to `flow.deadline` node `ask`"),
};

/** `flow.deadline` node `ask` — a human-in-the-loop pause (grammar 8.7). */
const flowDeadlineNodeAsk: runtime.NodeDescriptor = {
  flow: "flow.deadline",
  node: "ask",
  // Grammar 9.3, resolved: `retry` from exempt (Decision D102), `timeout` from exempt (Decision D102), `on_error` from `defaults:`.
  policy: {
    onError: "fail",
  },
  exempt: true,
  shapes: { input: flowDeadlineShape, state: stateShape, output: flowDeadlineNodeAskShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) =>
    runtime.runHuman(flowDeadlineNodeAskHuman, input, context, view),
  writes: [
    { field: "decision", channel: "approval", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.deadline` node `lapse` — an inline subprocess (grammar 8.2). */
const flowDeadlineNodeLapse: runtime.NodeDescriptor = {
  flow: "flow.deadline",
  node: "lapse",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowDeadlineShape, state: stateShape, output: flowDeadlineNodeLapseShape },
  input: () => ({}),
  run: async (input, context) => ({
    output: runtime.parseResult(
      flowDeadlineNodeLapseOutput,
      await runtime.runExec({
        command: [{ env: "OPS_BIN", site: "flow.deadline.node.lapse.exec.command" }, "/echo"],
        args: [
          ["{\"decision\":\"nobody answered in time\"}"],
        ],
        env: [],
        expectExit: [0],
        decoding: { envelope: [], decoded: ["decision"], empty: false },
      }, input, context),
      "the result of `flow.deadline` node `lapse`",
    ),
  }),
  writes: [
    { field: "decision", channel: "approval", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.deadline` — its nodes, its `start` edges, and the compiled graph. */
function flowDeadline() {
  return new StateGraph(State)
    .addNode("ask", (state: GraphState) => runtime.runNode(flowDeadlineNodeAsk, state), {
      ends: [END, "lapse"],
    })
    .addNode("lapse", (state: GraphState) => runtime.runNode(flowDeadlineNodeLapse, state), {
      ends: [END],
    })
    .addEdge(START, "ask")
    .compile();
}

/**
 * `flow.deadline`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowDeadlineGraph = flowDeadline();

/**
 * `flow.deadline` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowDeadlineBinding: runtime.SubflowBinding = {
  address: "flow.deadline",
  outputs: ["approval"],
  recursionLimit: 27,
  stream: (initial, options) =>
    flowDeadlineGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowDeadlineGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.direct ---

/** `flow.direct` node `sign` — `tool.sign` (grammar 8.4). */
const flowDirectNodeSign: runtime.NodeDescriptor = {
  flow: "flow.direct",
  node: "sign",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowDirectShape, state: stateShape, output: flowDirectNodeSignShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => ({
    output: runtime.parseResult(
      toolSignOutput,
      (
        await mesh.dispatchPlaced({
          placement: "mac",
          node: "flow.direct.sign",
          execution: view.run.execution.id,
          itemIndex: view.run.execution.item_index,
          path: runtime.instancePath(view, "sign"),
          inputs: input,
          signal: context.signal,
          stores: context.storeRecords,
        })
      ).output,
      "the result of `tool.sign`",
    ),
  }),
  writes: [
    { field: "signature", channel: "signature", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.direct` — its nodes, its `start` edges, and the compiled graph. */
function flowDirect() {
  return new StateGraph(State)
    .addNode("sign", (state: GraphState) => runtime.runNode(flowDirectNodeSign, state), {
      ends: [END],
    })
    .addEdge(START, "sign")
    .compile();
}

/**
 * `flow.direct`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowDirectGraph = flowDirect();

/**
 * `flow.direct` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowDirectBinding: runtime.SubflowBinding = {
  address: "flow.direct",
  outputs: ["signature"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowDirectGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowDirectGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.escalated ---

/** `flow.escalated` node `escalate` — `agent.escalator` (grammar 8.1). */
const flowEscalatedNodeEscalate: runtime.NodeDescriptor = {
  flow: "flow.escalated",
  node: "escalate",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowEscalatedShape, state: stateShape, output: flowEscalatedNodeEscalateShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => {
    const answer = await mesh.dispatchPlaced({
      placement: "mac",
      node: "flow.escalated.escalate",
      execution: view.run.execution.id,
      itemIndex: view.run.execution.item_index,
      path: runtime.instancePath(view, "escalate"),
      inputs: input,
      history: runtime.historyTurns(view.state["messages"] as unknown[]),
      policy: view.run.policy,
      signal: context.signal,
      stores: context.storeRecords,
    });
    return {
      output: runtime.parseResult(agentEscalatorOutput, answer.output, "the answer of `agent.escalator`"),
      history: answer.history,
      models: answer.models,
      toolDispatches: answer.toolDispatches,
    };
  },
  writes: [
    { field: "approval", channel: "approval", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.escalated` — its nodes, its `start` edges, and the compiled graph. */
function flowEscalated() {
  return new StateGraph(State)
    .addNode("escalate", (state: GraphState) => runtime.runNode(flowEscalatedNodeEscalate, state), {
      ends: [END],
    })
    .addEdge(START, "escalate")
    .compile();
}

/**
 * `flow.escalated`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowEscalatedGraph = flowEscalated();

/**
 * `flow.escalated` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowEscalatedBinding: runtime.SubflowBinding = {
  address: "flow.escalated",
  outputs: ["approval"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowEscalatedGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowEscalatedGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.escalation ---

/**
 * `flow.escalation` node `ask` — the pause it holds: what the human is shown, and what an answer has to fit (grammar 8.7, PRD 5.11).
 */
const flowEscalationNodeAskHuman: runtime.HumanDescriptor = {
  flow: "flow.escalation",
  node: "ask",
  schema: {
    "additionalProperties": false,
    "properties": {
      "decision": {
        "enum": [
          "approve",
          "reject"
        ],
        "type": "string"
      }
    },
    "required": [
      "decision"
    ],
    "type": "object"
  },
  parse: (payload) => runtime.parseResult(flowEscalationNodeAskOutput, payload, "the answer to `flow.escalation` node `ask`"),
};

/** `flow.escalation` node `ask` — a human-in-the-loop pause (grammar 8.7). */
const flowEscalationNodeAsk: runtime.NodeDescriptor = {
  flow: "flow.escalation",
  node: "ask",
  // Grammar 9.3, resolved: `retry` from exempt (Decision D102), `timeout` from exempt (Decision D102), `on_error` from `defaults:`.
  policy: {
    onError: "fail",
  },
  exempt: true,
  shapes: { input: flowEscalationShape, state: stateShape, output: flowEscalationNodeAskShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) =>
    runtime.runHuman(flowEscalationNodeAskHuman, input, context, view),
  writes: [
    { field: "decision", channel: "approval", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.escalation` — its nodes, its `start` edges, and the compiled graph. */
function flowEscalation() {
  return new StateGraph(State)
    .addNode("ask", (state: GraphState) => runtime.runNode(flowEscalationNodeAsk, state), {
      ends: [END],
    })
    .addEdge(START, "ask")
    .compile();
}

/**
 * `flow.escalation`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowEscalationGraph = flowEscalation();

/**
 * `flow.escalation` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowEscalationBinding: runtime.SubflowBinding = {
  address: "flow.escalation",
  outputs: ["approval"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowEscalationGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowEscalationGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.impatient ---

/** `flow.impatient` node `escalate` — `agent.escalator` (grammar 8.1). */
const flowImpatientNodeEscalate: runtime.NodeDescriptor = {
  flow: "flow.impatient",
  node: "escalate",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from the node, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 15000,
    onError: "fail",
  },
  shapes: { input: flowImpatientShape, state: stateShape, output: flowImpatientNodeEscalateShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => {
    const answer = await mesh.dispatchPlaced({
      placement: "mac",
      node: "flow.impatient.escalate",
      execution: view.run.execution.id,
      itemIndex: view.run.execution.item_index,
      path: runtime.instancePath(view, "escalate"),
      inputs: input,
      history: runtime.historyTurns(view.state["messages"] as unknown[]),
      policy: view.run.policy,
      signal: context.signal,
      stores: context.storeRecords,
    });
    return {
      output: runtime.parseResult(agentEscalatorOutput, answer.output, "the answer of `agent.escalator`"),
      history: answer.history,
      models: answer.models,
      toolDispatches: answer.toolDispatches,
    };
  },
  writes: [
    { field: "approval", channel: "approval", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.impatient` — its nodes, its `start` edges, and the compiled graph. */
function flowImpatient() {
  return new StateGraph(State)
    .addNode("escalate", (state: GraphState) => runtime.runNode(flowImpatientNodeEscalate, state), {
      ends: [END],
    })
    .addEdge(START, "escalate")
    .compile();
}

/**
 * `flow.impatient`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowImpatientGraph = flowImpatient();

/**
 * `flow.impatient` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowImpatientBinding: runtime.SubflowBinding = {
  address: "flow.impatient",
  outputs: ["approval"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowImpatientGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowImpatientGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.release ---

/** `flow.release` node `sign` — `agent.signer` (grammar 8.1). */
const flowReleaseNodeSign: runtime.NodeDescriptor = {
  flow: "flow.release",
  node: "sign",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowReleaseShape, state: stateShape, output: flowReleaseNodeSignShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => {
    const answer = await mesh.dispatchPlaced({
      placement: "mac",
      node: "flow.release.sign",
      execution: view.run.execution.id,
      itemIndex: view.run.execution.item_index,
      path: runtime.instancePath(view, "sign"),
      inputs: input,
      history: runtime.historyTurns(view.state["messages"] as unknown[]),
      policy: view.run.policy,
      signal: context.signal,
      stores: context.storeRecords,
    });
    return {
      output: runtime.parseResult(agentSignerOutput, answer.output, "the answer of `agent.signer`"),
      history: answer.history,
      models: answer.models,
      toolDispatches: answer.toolDispatches,
    };
  },
  writes: [
    { field: "signature", channel: "signature", reduce: "set" },
  ],
  edges: [
    { to: "stamp" },
  ],
};

/** `flow.release` node `stamp` — `tool.notarize` (grammar 8.4). */
const flowReleaseNodeStamp: runtime.NodeDescriptor = {
  flow: "flow.release",
  node: "stamp",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowReleaseShape, state: stateShape, output: flowReleaseNodeStampShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context) => ({ output: await toolNotarize(input, context) }),
  writes: [
    { field: "ticket", channel: "ticket", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.release` — its nodes, its `start` edges, and the compiled graph. */
function flowRelease() {
  return new StateGraph(State)
    .addNode("sign", (state: GraphState) => runtime.runNode(flowReleaseNodeSign, state), {
      ends: ["stamp"],
    })
    .addNode("stamp", (state: GraphState) => runtime.runNode(flowReleaseNodeStamp, state), {
      ends: [END],
    })
    .addEdge(START, "sign")
    .compile();
}

/**
 * `flow.release`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowReleaseGraph = flowRelease();

/**
 * `flow.release` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowReleaseBinding: runtime.SubflowBinding = {
  address: "flow.release",
  outputs: ["signature", "ticket"],
  recursionLimit: 27,
  stream: (initial, options) =>
    flowReleaseGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowReleaseGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.retried ---

/** `flow.retried` node `sign` — `agent.signer` (grammar 8.1). */
const flowRetriedNodeSign: runtime.NodeDescriptor = {
  flow: "flow.retried",
  node: "sign",
  // Grammar 9.3, resolved: `retry` from the node, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    retry: {
      max: 2,
      backoffMs: 1,
      multiplier: 2,
      jitter: true,
    },
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowRetriedShape, state: stateShape, output: flowRetriedNodeSignShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => {
    const answer = await mesh.dispatchPlaced({
      placement: "mac",
      node: "flow.retried.sign",
      execution: view.run.execution.id,
      itemIndex: view.run.execution.item_index,
      path: runtime.instancePath(view, "sign"),
      inputs: input,
      history: runtime.historyTurns(view.state["messages"] as unknown[]),
      policy: view.run.policy,
      signal: context.signal,
      stores: context.storeRecords,
    });
    return {
      output: runtime.parseResult(agentSignerOutput, answer.output, "the answer of `agent.signer`"),
      history: answer.history,
      models: answer.models,
      toolDispatches: answer.toolDispatches,
    };
  },
  writes: [
    { field: "signature", channel: "signature", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.retried` — its nodes, its `start` edges, and the compiled graph. */
function flowRetried() {
  return new StateGraph(State)
    .addNode("sign", (state: GraphState) => runtime.runNode(flowRetriedNodeSign, state), {
      ends: [END],
    })
    .addEdge(START, "sign")
    .compile();
}

/**
 * `flow.retried`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowRetriedGraph = flowRetried();

/**
 * `flow.retried` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowRetriedBinding: runtime.SubflowBinding = {
  address: "flow.retried",
  outputs: ["signature"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowRetriedGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowRetriedGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.signed_off ---

/** `flow.signed_off` node `sign` — `agent.signer` (grammar 8.1). */
const flowSignedOffNodeSign: runtime.NodeDescriptor = {
  flow: "flow.signed_off",
  node: "sign",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowSignedOffShape, state: stateShape, output: flowSignedOffNodeSignShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => {
    const answer = await mesh.dispatchPlaced({
      placement: "mac",
      node: "flow.signed_off.sign",
      execution: view.run.execution.id,
      itemIndex: view.run.execution.item_index,
      path: runtime.instancePath(view, "sign"),
      inputs: input,
      history: runtime.historyTurns(view.state["messages"] as unknown[]),
      policy: view.run.policy,
      signal: context.signal,
      stores: context.storeRecords,
    });
    return {
      output: runtime.parseResult(agentSignerOutput, answer.output, "the answer of `agent.signer`"),
      history: answer.history,
      models: answer.models,
      toolDispatches: answer.toolDispatches,
    };
  },
  writes: [
    { field: "signature", channel: "signature", reduce: "set" },
  ],
  edges: [
    { to: "approve" },
  ],
};

/**
 * `flow.signed_off` node `approve` — the pause it holds: what the human is shown, and what an answer has to fit (grammar 8.7, PRD 5.11).
 */
const flowSignedOffNodeApproveHuman: runtime.HumanDescriptor = {
  flow: "flow.signed_off",
  node: "approve",
  schema: {
    "additionalProperties": false,
    "properties": {
      "decision": {
        "enum": [
          "approve",
          "reject"
        ],
        "type": "string"
      }
    },
    "required": [
      "decision"
    ],
    "type": "object"
  },
  parse: (payload) => runtime.parseResult(flowSignedOffNodeApproveOutput, payload, "the answer to `flow.signed_off` node `approve`"),
};

/**
 * `flow.signed_off` node `approve` — a human-in-the-loop pause (grammar 8.7).
 */
const flowSignedOffNodeApprove: runtime.NodeDescriptor = {
  flow: "flow.signed_off",
  node: "approve",
  // Grammar 9.3, resolved: `retry` from exempt (Decision D102), `timeout` from exempt (Decision D102), `on_error` from `defaults:`.
  policy: {
    onError: "fail",
  },
  exempt: true,
  shapes: { input: flowSignedOffShape, state: stateShape, output: flowSignedOffNodeApproveShape },
  input: (roots, view) => ({
    "signature": runtime.toJson(runtime.evaluate("state.signature", roots)),
  }),
  run: async (input, context, view) =>
    runtime.runHuman(flowSignedOffNodeApproveHuman, input, context, view),
  writes: [
    { field: "decision", channel: "approval", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.signed_off` — its nodes, its `start` edges, and the compiled graph. */
function flowSignedOff() {
  return new StateGraph(State)
    .addNode("sign", (state: GraphState) => runtime.runNode(flowSignedOffNodeSign, state), {
      ends: ["approve"],
    })
    .addNode("approve", (state: GraphState) => runtime.runNode(flowSignedOffNodeApprove, state), {
      ends: [END],
    })
    .addEdge(START, "sign")
    .compile();
}

/**
 * `flow.signed_off`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowSignedOffGraph = flowSignedOff();

/**
 * `flow.signed_off` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowSignedOffBinding: runtime.SubflowBinding = {
  address: "flow.signed_off",
  outputs: ["signature"],
  recursionLimit: 27,
  stream: (initial, options) =>
    flowSignedOffGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowSignedOffGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.stamped ---

/** `flow.stamped` node `stamp` — `tool.stamp` (grammar 8.4). */
const flowStampedNodeStamp: runtime.NodeDescriptor = {
  flow: "flow.stamped",
  node: "stamp",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowStampedShape, state: stateShape, output: flowStampedNodeStampShape },
  input: (roots, view) => ({
    "path": runtime.toJson(runtime.evaluate("input.path", roots)),
  }),
  run: async (input, context, view) => ({
    output: runtime.parseResult(
      toolStampOutput,
      (
        await mesh.dispatchPlaced({
          placement: "mac",
          node: "flow.stamped.stamp",
          execution: view.run.execution.id,
          itemIndex: view.run.execution.item_index,
          path: runtime.instancePath(view, "stamp"),
          inputs: input,
          signal: context.signal,
          stores: context.storeRecords,
        })
      ).output,
      "the result of `tool.stamp`",
    ),
  }),
  writes: [
    { field: "stamped", channel: "signature", reduce: "set" },
  ],
  edges: [
    { to: END },
  ],
};

/** `flow.stamped` — its nodes, its `start` edges, and the compiled graph. */
function flowStamped() {
  return new StateGraph(State)
    .addNode("stamp", (state: GraphState) => runtime.runNode(flowStampedNodeStamp, state), {
      ends: [END],
    })
    .addEdge(START, "stamp")
    .compile();
}

/**
 * `flow.stamped`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowStampedGraph = flowStamped();

/**
 * `flow.stamped` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowStampedBinding: runtime.SubflowBinding = {
  address: "flow.stamped",
  outputs: ["signature"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowStampedGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowStampedGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

// --- flow.watched ---

/**
 * `flow.watched` node `watch` — the fan-out it dispatches (grammar 8.6). `over` resolves against `input.paths`, and `path` is what an instance's own bindings call the item.
 */
const flowWatchedNodeWatchMap: runtime.MapDescriptor = {
  node: "watch",
  as: "path",
  source: {
    path: "input.paths",
    shape: "any",
  },
  maxConcurrency: 4,
  onItemError: "fail",
  routes: [
    {
      target: "flow.signed_off",
      maxConcurrency: 4,
      detach: false,
      itemShape: "string",
      input: (roots) => ({
        "path": runtime.toJson(runtime.evaluate("path", roots)),
      }),
      run: async (input, context, site) =>
        runtime.runSubflow(flowSignedOffBinding, {
          inputs: input as Record<string, unknown>,
          execution: site.execution,
          path: site.path,
          signal: context.signal,
        }),
      writes: [
        { field: "signature", channel: "signatures", reduce: "append" },
      ],
    },
  ],
};

/** `flow.watched` node `watch` — a fan-out (grammar 8.6). */
const flowWatchedNodeWatch: runtime.NodeDescriptor = {
  flow: "flow.watched",
  node: "watch",
  // Grammar 9.3, resolved: `retry` from the built-in, `timeout` from `defaults:`, `on_error` from `defaults:`.
  policy: {
    timeoutMs: 60000,
    onError: "fail",
  },
  shapes: { input: flowWatchedShape, state: stateShape, output: "any" },
  input: (_roots, view) => runtime.mapPlan(flowWatchedNodeWatchMap, view),
  run: async (input, context) =>
    runtime.runMap(flowWatchedNodeWatchMap, input as runtime.MapPlan, context),
  writes: [],
  edges: [
    { to: END },
  ],
};

/** `flow.watched` — its nodes, its `start` edges, and the compiled graph. */
function flowWatched() {
  return new StateGraph(State)
    .addNode("watch", (state: GraphState) => runtime.runNode(flowWatchedNodeWatch, state), {
      ends: [END],
    })
    .addEdge(START, "watch")
    .compile();
}

/**
 * `flow.watched`, compiled once. Building it at import is also what checks it: a state model LangGraph refuses, or an edge to a node that is not registered, fails here rather than at the first invocation.
 */
const flowWatchedGraph = flowWatched();

/**
 * `flow.watched` as a module: what a `flow:` node instantiates and a `map` dispatches to (grammar 7.5, 8.5).
 */
const flowWatchedBinding: runtime.SubflowBinding = {
  address: "flow.watched",
  outputs: ["signatures"],
  recursionLimit: 26,
  stream: (initial, options) =>
    flowWatchedGraph.stream(initial, {
      ...options,
      streamMode: "values",
      outputKeys: flowWatchedGraph.outputChannels,
    }) as unknown as Promise<AsyncIterable<runtime.GraphStateLike>>,
};

/**
 * How a declared flow input reads a command-line value (grammar 13.2).
 *
 * `json` is every structured shape — an object, an array, a tagged union — for
 * which the one honest reading of a shell argument is the document it spells.
 */
export type InputKind = "string" | "integer" | "number" | "boolean" | "json";

/** One compiled flow: what it takes, what it answers, and how to run it. */
export interface CompiledFlow {
  /** Its typed address (grammar 2.2). */
  readonly address: string;
  /** The fields its `inputs:` declares (grammar 7.5). */
  readonly inputs: readonly string[];
  /**
   * What each declared input *is*, so a `--input k=v` argument can be read as
   * the type the field declares rather than reaching Zod as text (grammar 13.2).
   */
  readonly inputKinds: Readonly<Record<string, InputKind>>;
  /** The fields its `outputs:` declares, each read from the channel of that name. */
  readonly outputs: readonly string[];
  /**
   * The `scope: session` stores this flow **reaches**, under the relation
   * grammar 7.7 fixes — its own nodes, its maps' dispatch targets, the flows it
   * instantiates, and the stores of every agent it reaches.
   *
   * A run of this flow needs a session identity exactly when this list is not
   * empty (grammar 11.3): a declared trigger supplies it through `session_key:`,
   * and the CLI through `--session`. Checked at run start rather than at
   * validate, for the reason env-ref presence is: the value does not exist until
   * the invocation does.
   */
  readonly sessionStores: readonly string[];
  /** The superstep ceiling a run of it takes by default. */
  readonly recursionLimit: number;
  /** Parse an invocation's inputs against the flow's own schema (grammar 13.2). */
  parse(inputs: unknown): Record<string, unknown>;
  /**
   * Invoke the compiled graph, answering with the state after each superstep.
   *
   * A stream rather than a plain invocation because of what a **failure** must
   * leave behind. LangGraph's `invoke` is this stream with the last value kept,
   * and an error thrown out of it discards the state it was keeping — the
   * routing trace of PRD 5.3 with it, since nothing here is checkpointed. Taking
   * the supersteps one at a time keeps every one that did complete, which is
   * what `runFlow` reports the failure with.
   */
  stream(
    initial: Record<string, unknown>,
    options: { recursionLimit: number },
  ): Promise<AsyncIterable<GraphState>>;
}

/**
 * Every flow this composition declares, by address.
 *
 * PRD 5.11 makes manual invocation universal — every flow is runnable whether or
 * not a `manual` trigger names it (Decision D64) — so the registry is every
 * flow rather than every triggered one.
 */
export const flows: Readonly<Record<string, CompiledFlow>> = {
  "flow.abandoned": {
    address: "flow.abandoned",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["approval"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowAbandonedInputs, inputs, "the `inputs:` of `flow.abandoned`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowAbandonedGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowAbandonedGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.batch": {
    address: "flow.batch",
    inputs: ["paths"],
    inputKinds: { "paths": "json", },
    outputs: ["signatures"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowBatchInputs, inputs, "the `inputs:` of `flow.batch`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowBatchGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowBatchGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.conversation": {
    address: "flow.conversation",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["signature"],
    sessionStores: [],
    recursionLimit: 27,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowConversationInputs, inputs, "the `inputs:` of `flow.conversation`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowConversationGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowConversationGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.deadline": {
    address: "flow.deadline",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["approval"],
    sessionStores: [],
    recursionLimit: 27,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowDeadlineInputs, inputs, "the `inputs:` of `flow.deadline`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowDeadlineGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowDeadlineGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.direct": {
    address: "flow.direct",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["signature"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowDirectInputs, inputs, "the `inputs:` of `flow.direct`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowDirectGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowDirectGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.escalated": {
    address: "flow.escalated",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["approval"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowEscalatedInputs, inputs, "the `inputs:` of `flow.escalated`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowEscalatedGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowEscalatedGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.escalation": {
    address: "flow.escalation",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["approval"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowEscalationInputs, inputs, "the `inputs:` of `flow.escalation`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowEscalationGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowEscalationGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.impatient": {
    address: "flow.impatient",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["approval"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowImpatientInputs, inputs, "the `inputs:` of `flow.impatient`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowImpatientGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowImpatientGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.release": {
    address: "flow.release",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["signature", "ticket"],
    sessionStores: [],
    recursionLimit: 27,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowReleaseInputs, inputs, "the `inputs:` of `flow.release`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowReleaseGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowReleaseGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.retried": {
    address: "flow.retried",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["signature"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowRetriedInputs, inputs, "the `inputs:` of `flow.retried`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowRetriedGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowRetriedGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.signed_off": {
    address: "flow.signed_off",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["signature"],
    sessionStores: [],
    recursionLimit: 27,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowSignedOffInputs, inputs, "the `inputs:` of `flow.signed_off`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowSignedOffGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowSignedOffGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.stamped": {
    address: "flow.stamped",
    inputs: ["path"],
    inputKinds: { "path": "string", },
    outputs: ["signature"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowStampedInputs, inputs, "the `inputs:` of `flow.stamped`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowStampedGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowStampedGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
  "flow.watched": {
    address: "flow.watched",
    inputs: ["paths"],
    inputKinds: { "paths": "json", },
    outputs: ["signatures"],
    sessionStores: [],
    recursionLimit: 26,
    parse: (inputs: unknown) =>
      runtime.parseResult(flowWatchedInputs, inputs, "the `inputs:` of `flow.watched`") as Record<string, unknown>,
    stream: (initial, options) =>
      flowWatchedGraph.stream(initial, {
        ...options,
        streamMode: "values",
        outputKeys: flowWatchedGraph.outputChannels,
      }) as unknown as Promise<AsyncIterable<GraphState>>,
  },
};

/** What one run produced. */
export interface FlowRun {
  /** The flow's `outputs:`, materialized from state at quiescence (grammar 7.6.3). */
  readonly outputs: Record<string, unknown>;
  /** Every routing decision the run made, in step order (PRD 5.3). */
  readonly trace: readonly runtime.TraceEntry[];
  /** The whole state at quiescence. */
  readonly state: GraphState;
}

/**
 * Why a flow that reaches a `scope: session` store refuses a run that arrived
 * with no session identity (grammar 11.3, 13.2).
 *
 * One sentence in one place, because it is raised from two and the two must not
 * drift: `src/cli.ts` raises it as a **usage** error, before a `run` starts
 * anything, because a missing `--session` is an argument the caller has to add —
 * the same class as an unknown `--input` name or an absent `${ENV}`, which
 * grammar 11.3 says outright by likening this check to env-ref presence (§4.3);
 * `runFlow` raises it for every other caller, where the key arrives per
 * invocation.
 *
 * Named by the store rather than by the flow, because the store is what the
 * author has to look at. And three ways a run arrives with none are named,
 * because two of them are advice a reader has already taken: a declared
 * `session_key:` that *evaluated* to the empty string — an absent header, a
 * payload member that was not sent — is an identity-less run whose trigger does
 * declare one, and a message offering only the two remedies would send that
 * reader to look at a line that is already there (PRD G3).
 */
export function sessionRefusal(address: string, stores: readonly string[]): string {
  return `\`${address}\` reaches ${stores.map((store) => `\`${store}\``).join(", ")}, which ${stores.length === 1 ? "is" : "are"} \`scope: session\`, so this run needs a session identity and arrived with none: pass \`--session <key>\` to \`agent-compose run\`, or declare \`session_key:\` on the trigger that starts it — and where one is declared, it answered the empty string for this invocation (grammar 11.3, 13.2)`;
}

/**
 * Run one flow to quiescence and materialize its outputs.
 *
 * This is the invocation surface `agent-compose run` and the generated `serve`
 * app are built on (PRD 5.11's `start`), and what an ejected project calls
 * directly. The inputs are parsed against the flow's own `inputs:` schema before
 * anything runs, which is where an invocation that the flow cannot accept is
 * refused by field name (grammar 13.2).
 *
 * A run that produces no answer raises `runtime.FlowFailure`, which carries the
 * trace it did make and the original error as its `cause` — see
 * `CompiledFlow.stream` for why the run is streamed to keep it. Both of the ways
 * that happens raise it: a run that never reached quiescence, and a run that
 * reached quiescence holding no value for one of its `outputs:` fields
 * (grammar 10.1, Decision D78). The second is the one whose trace is complete —
 * every step landed — so dropping it there would lose the whole routing record
 * of a run that made one.
 *
 * A third way is a `human` node (grammar 8.7): a run that reaches one and was
 * started with **no answer surface** stops there, and the `FlowFailure` carries
 * a `runtime.HumanInterrupt` on its `cause` chain — `runtime.interruptOf` is how
 * a caller tells that outcome from a failure. `resumable: true` is what says an
 * answer surface is attached, and there are two callers that pass it, one per
 * surface (PRD §9.21): `src/serve.ts`, whose `POST /executions/:id/resume`
 * delivers the answer, and `src/cli.ts`, when the `run` it is serving may ask
 * at the terminal it was launched from.
 *
 * # Every invocation is journaled
 *
 * PRD resolved q26–q29: the run's effects are written to this project's journal
 * as they happen, and `resume: true` re-runs the graph consuming that record
 * read-only up to the frontier. Journaling is unconditional — every target this
 * compiler builds is process-local and binds the SQLite journal beside the
 * project (resolved q27) — so a caller that says nothing about durability still
 * gets it. `docs/durability.md` is normative.
 */
export async function runFlow(
  address: string,
  inputs: unknown = {},
  options: {
    readonly executionId?: string;
    readonly sessionKey?: string;
    readonly recursionLimit?: number;
    /**
     * Whether something is standing by to answer a `human` pause this run
     * reaches (grammar 8.7, PRD 5.11).
     *
     * `false` — the default, and what an `agent-compose run` with nobody to ask
     * leaves it at — makes a pause the end of the run rather than a wait
     * nothing can settle. `src/cli.ts` passes `true` where the run may ask at
     * its own standard input, and `src/serve.ts` passes it for the resume
     * route.
     */
    readonly resumable?: boolean;
    /**
     * What started this execution, for the journal's lifecycle row.
     *
     * `manual` where nothing says otherwise, which is every `agent-compose run`
     * and every `manual` trigger; `src/serve.ts` passes the `http` trigger's own
     * name. Recorded and never dispatched on — recovery replays executions, it
     * does not re-fire triggers (PRD resolved q28).
     */
    readonly trigger?: string;
    /**
     * The completion webhook of an `async` `http` trigger, resolved against the
     * request that started this execution (grammar 13.3).
     *
     * `src/serve.ts` passes it and nothing else does. It goes on the journal's
     * lifecycle row because the process that finishes an execution need not be
     * the one that started it: a `serve` that restarts mid-run recovers the
     * execution and has to be able to call the caller back
     * (`docs/durability.md` §6.1).
     */
    readonly callback?: string;
    /**
     * Whether this is a **resumed** generation of an execution the journal
     * already holds (PRD resolved q29).
     *
     * `true` makes every effect site consult the journal before it calls the
     * world: a recorded answer is returned byte for byte and nothing is
     * re-issued, until the frontier — the first effect the journal does not
     * hold — where the execution goes live again. `executionId` names which
     * execution, and `inputs`/`sessionKey` must be the ones the journal
     * recorded, which is why the two callers that resume read them back off the
     * lifecycle row rather than composing them again.
     */
    readonly resume?: boolean;
    /**
     * Called once, **immediately before this run closes its lifecycle row**,
     * and awaited.
     *
     * The one caller is `src/serve.ts`, and what it does with it is journal the
     * `settled` webhook's intent while the row is still open
     * (`docs/durability.md` §3.7): a delivery recorded after the row closed is
     * one no restart can find — `recover` sees no open execution and the
     * delivery ledger has no row — so a process killed in that window would owe
     * a caller a push nothing would ever send. It is passed what the run
     * produced, or the error that ended it, because the report a webhook
     * carries is that answer and this is the last moment it exists inside the
     * run.
     *
     * It is **not** called for a run whose row stays open — a `human` pause
     * nobody could answer, a replay divergence — which is what keeps resolved
     * q29's "a divergence fires nothing" a property of the structure rather
     * than of a check somebody remembered to write.
     *
     * Its own failure is its own: a webhook that could not be recorded is not
     * the run's outcome, and letting it through here would replace what a run
     * really did with what a receiver's ledger could not hold.
     */
    readonly closing?: (produced: FlowRun | undefined, error: unknown) => Promise<void>;
  } = {},
): Promise<FlowRun> {
  const flow = flows[address];
  if (flow === undefined) {
    throw new Error(
      `\`${address}\` is not a flow of this composition: ${Object.keys(flows).join(", ")}`,
    );
  }
  const parsed = flow.parse(inputs);
  const ceiling = options.recursionLimit ?? flow.recursionLimit;
  const sessionKey = options.sessionKey ?? "";
  // Grammar 11.3, checked where the value first exists: a flow that reaches a
  // `scope: session` store keys off the identity its trigger supplies, and a run
  // started without one would silently address a partition named by the empty
  // string. `src/cli.ts` decides the same thing one step earlier for a `run`,
  // where the identity is a command-line argument; this is the guard for every
  // caller it cannot stand in for — a `serve` request, an ejected invocation —
  // where the key arrives per invocation and its absence really is a failure of
  // that run rather than of the command.
  if (sessionKey === "" && flow.sessionStores.length > 0) {
    throw new Error(sessionRefusal(address, flow.sessionStores));
  }
  const executionId = options.executionId ?? `exec_${globalThis.crypto.randomUUID()}`;
  // Before the graph is streamed, so an execution the process dies in the middle
  // of already has a row saying it was open (PRD resolved q28).
  await runtime.openExecution({
    execution: executionId,
    flow: address,
    trigger: options.trigger ?? "manual",
    inputs: parsed,
    sessionKey,
    ...(options.callback === undefined ? {} : { callback: options.callback }),
    ...(options.resume === true ? { resuming: true } : {}),
  });
  try {
    const produced = await quiesceFlow(address, flow, parsed, ceiling, sessionKey, executionId, options);
    // A run that reached quiescence may still be holding a divergence raised
    // where nothing could throw it — a detached `map` delivery, which grammar 8.6
    // rule 7 says the flow instance does not wait for. PRD resolved q29 makes a
    // divergence un-absorbable by any policy at any nesting depth, and `detach:`
    // is one, so it fails the resume here rather than being reported as a
    // completion the record does not support.
    const diverged = runtime.latchedDivergence(executionId);
    if (diverged !== undefined) throw diverged;
    // The last moment before the row closes, and what `closing` is for. Its own
    // failure is swallowed rather than allowed to travel: it would report a run
    // that produced its outputs as one that failed, over a webhook the status
    // route backstops (see the option's own comment).
    if (options.closing !== undefined) {
      try {
        await options.closing(produced, undefined);
      } catch {
        // Said by the hook, where a hook has something to say.
      }
    }
    runtime.settleExecution(executionId);
    return produced;
  } catch (error) {
    // Which of the three closing rows this is — `failed`, or none at all
    // because the run is parked at a `human` pause and is exactly what a resume
    // exists for — is `runtime.settleExecution`'s to decide, off the same
    // `runtime.interruptOf` this function's own callers read.
    //
    // …which is also what decides whether anything is *closing*: the hook is
    // the last moment before a row closes, and a row that stays open has not
    // reached one. `runtime.staysOpen` is the predicate `settleExecution` is
    // about to ask, asked here so the two cannot answer differently.
    if (options.closing !== undefined && !runtime.staysOpen(executionId, error)) {
      try {
        await options.closing(undefined, error);
      } catch {
        // …and here it would replace the error the run really produced.
      }
    }
    runtime.settleExecution(executionId, error);
    throw error;
  } finally {
    runtime.closeExecution(executionId);
  }
}

/** [`runFlow`]'s body, with the journal's lifecycle row already open. */
async function quiesceFlow(
  address: string,
  flow: CompiledFlow,
  parsed: Record<string, unknown>,
  ceiling: number,
  sessionKey: string,
  executionId: string,
  options: { readonly resumable?: boolean; readonly resume?: boolean },
): Promise<FlowRun> {
  // Opened before the graph is streamed, so a status route asked the instant
  // after `start` answered already has somewhere to read this run's pauses from
  // (grammar 8.7, PRD 5.11). Every instance nested inside the run registers
  // against the same execution id and is told apart by its instance path.
  runtime.openHumanWaits(executionId, options.resumable === true);
  // `scope: execution` means what it says: whatever this run's own stores held
  // is released when the run ends, however it ended (PRD 5.8, grammar 11.1).
  // A `serve` process runs many executions, so a store that stayed open would
  // be both a leak and a lifetime the composition did not declare. A pause the
  // run was holding goes the same way and for the same reason: a wait that
  // outlived its run would be one a resume could still be delivered to, with
  // no graph left to receive it (grammar 8.7).
  //
  // **Unless the run has not ended.** An execution whose journal row stays open
  // — parked at a `human` pause with nobody to answer it, or stopped by a
  // divergence — is one a resume replays, and `runtime.staysOpen` is the very
  // predicate `runtime.settleExecution` decides that by. What such a run owns
  // *on disk* has to still be there when the resumed generation reads past the
  // frontier: a replayed write is never applied a second time, so a partition
  // this generation deleted would answer an empty `get` about something the
  // execution wrote, with nothing comparing unequal to catch it
  // (`docs/durability.md` §5).
  const release = async (outcome: unknown): Promise<void> => {
    runtime.releaseHumanWaits(executionId);
    // …and the placement waits, for the same reason one level out: a dispatch
    // nothing is waiting for is work a worker could still take, whose result
    // would be posted against a node execution that is gone
    // (`docs/distributed.md` §6.1, `./mesh.ts`).
    mesh.releasePlacementWaits(executionId);
    // And the deliveries nothing joined, on the two ways out where one still in
    // flight can change what this function has to decide.
    //
    // A detached `map` delivery is journaled when it answers, so a generation
    // that walks out from under one in flight leaves an effect with no record —
    // which the generation that resumes this execution issues a second time
    // (`docs/durability.md` §3.2). That is the **parked** half, and for a run
    // that ended it costs nothing, because nothing will resume it.
    //
    // The **resuming** half is about the answer itself. A delivery is the one
    // place a `runtime.ReplayDivergence` has nothing to be thrown to, so it is
    // latched against the execution (PRD resolved q29) — and a latch is exactly
    // what makes `runtime.staysOpen` true. A delivery still working through its
    // permits when the graph quiesced can latch one *after* a reading taken
    // here, and a reading taken before that is a run closed `completed` over a
    // delivery the record describes and nobody made, or a `scope: execution`
    // blob partition removed under the very resume §5 promises it to. So a
    // generation that is consuming a record waits for them: a divergence can
    // arise on no other kind (a first generation has no record to disagree
    // with), which is why this is not a wait every run pays.
    //
    // Grammar 8.6 rule 7 is untouched either way: the join returned at
    // dispatch, the trace entry was written without it, and this is `runFlow`
    // on its way out of a run that has already stopped advancing.
    if (runtime.staysOpen(executionId, outcome) || options.resume === true) {
      await runtime.settleDetached(executionId);
    }
    // Read **after** the deliveries have settled, so it is the answer the whole
    // execution gives rather than the one it gave at the instant the graph
    // quiesced.
    const parked = runtime.staysOpen(executionId, outcome);
    stores.releaseExecution(executionId, parked);
  };
  // `runtime.quiesce` keeps the last state each superstep produced, which is
  // what makes a failure's trace survive; the one failure it restates on the way
  // out is LangGraph stopping the run at the ceiling. Its answer carries the
  // run's own failure rather than throwing it, so the release reads that error
  // on the settled path and the thrown one on the other — the same outcome
  // either way, which is what a `finally` could not have been told.
  const { state, error } = await runtime
    .quiesce(
      flow,
      {
        $run: {
          ...runtime.emptyRun(),
          input: parsed,
          execution: { id: executionId, session_key: sessionKey },
        },
      },
      ceiling,
    )
    .then(
      async (reached) => {
        await release(reached.error);
        return reached;
      },
      async (thrown: unknown) => {
        await release(thrown);
        throw thrown;
      },
    );
  if (error !== undefined) {
    throw new runtime.FlowFailure(
      address,
      "did not run to quiescence",
      runtime.failedTrace(state, error),
      error,
    );
  }
  if (state === undefined) {
    // Unreachable: `streamMode: "values"` emits the state the run started from
    // before any node has run. A run with no state at all is still not one this
    // function can answer for, and saying so beats reading `undefined` as empty.
    throw new Error(`\`${address}\` produced no state`);
  }
  const quiesced = state;

  const outputs: Record<string, unknown> = {};
  try {
    for (const field of flow.outputs) {
      outputs[field] = runtime.channelValue(
        quiesced as unknown as Record<string, unknown>,
        field,
        `\`${address}\`'s output field \`${field}\``,
      );
    }
  } catch (error) {
    // Inside the same record as every other way a run fails to answer: the run
    // got all the way here, so `$run.trace` holds every step it took, and that
    // is the routing record a reader wants most when the flow cannot say what it
    // produced (PRD 5.3).
    throw new runtime.FlowFailure(
      address,
      "reached quiescence without an output",
      quiesced.$run.trace,
      error,
    );
  }
  return { outputs, trace: quiesced.$run.trace, state: quiesced };
}

/**
 * A new builder over this composition's state model.
 *
 * Every flow's topology is added to one of these. Constructing it is also what
 * proves the state model is a shape LangGraph accepts — a channel spec it
 * refuses fails here rather than at the first invocation.
 */
export function createBuilder() {
  return new StateGraph(State);
}

/**
 * What each placed call site does, for the process that **executes** it
 * (`docs/distributed.md` §3.2, §7.4).
 *
 * The other side of `mesh.dispatchPlaced`. A hub journals a dispatch and waits;
 * the worker that takes it runs `./worker-node.ts`, which looks the dispatch's
 * `node` up here and runs exactly the activity this node would have run had
 * nothing been placed. One lowering, two processes — which is what keeps a
 * placed node from meaning something different from an unplaced one.
 *
 * Keyed by the address a dispatch names: `<flow>.<node>` for an `agent:` or
 * `function:` node, and the component's own address for a `map` dispatch target
 * (grammar §14.1's three ways a graph reaches a placed component).
 */
export const placedNodes: Readonly<Record<string, mesh.PlacedRun>> = {
  "agent.signer":
    async (input, context, site) => {
      const answer = await runtime.callAgent(
        agentSigner,
        input,
        site.history ?? [],
        context,
        { path: site.path, policy: site.policy },
      );
      return {
        output: answer.output,
        history: answer.history,
        models: answer.models,
        toolDispatches: answer.toolDispatches,
      };
    },
  "flow.abandoned.escalate":
    async (input, context, site) => {
      const answer = await runtime.callAgent(
        agentEscalator,
        input,
        site.history ?? [],
        context,
        { path: site.path, policy: site.policy },
      );
      return {
        output: answer.output,
        history: answer.history,
        models: answer.models,
        toolDispatches: answer.toolDispatches,
      };
    },
  "flow.conversation.sign":
    async (input, context, site) => {
      const answer = await runtime.callAgent(
        agentSigner,
        input,
        site.history ?? [],
        context,
        { path: site.path, policy: site.policy },
      );
      return {
        output: answer.output,
        history: answer.history,
        models: answer.models,
        toolDispatches: answer.toolDispatches,
      };
    },
  "flow.direct.sign":
    async (input, context) => ({ output: await toolSign(input, context) }),
  "flow.escalated.escalate":
    async (input, context, site) => {
      const answer = await runtime.callAgent(
        agentEscalator,
        input,
        site.history ?? [],
        context,
        { path: site.path, policy: site.policy },
      );
      return {
        output: answer.output,
        history: answer.history,
        models: answer.models,
        toolDispatches: answer.toolDispatches,
      };
    },
  "flow.impatient.escalate":
    async (input, context, site) => {
      const answer = await runtime.callAgent(
        agentEscalator,
        input,
        site.history ?? [],
        context,
        { path: site.path, policy: site.policy },
      );
      return {
        output: answer.output,
        history: answer.history,
        models: answer.models,
        toolDispatches: answer.toolDispatches,
      };
    },
  "flow.release.sign":
    async (input, context, site) => {
      const answer = await runtime.callAgent(
        agentSigner,
        input,
        site.history ?? [],
        context,
        { path: site.path, policy: site.policy },
      );
      return {
        output: answer.output,
        history: answer.history,
        models: answer.models,
        toolDispatches: answer.toolDispatches,
      };
    },
  "flow.retried.sign":
    async (input, context, site) => {
      const answer = await runtime.callAgent(
        agentSigner,
        input,
        site.history ?? [],
        context,
        { path: site.path, policy: site.policy },
      );
      return {
        output: answer.output,
        history: answer.history,
        models: answer.models,
        toolDispatches: answer.toolDispatches,
      };
    },
  "flow.signed_off.sign":
    async (input, context, site) => {
      const answer = await runtime.callAgent(
        agentSigner,
        input,
        site.history ?? [],
        context,
        { path: site.path, policy: site.policy },
      );
      return {
        output: answer.output,
        history: answer.history,
        models: answer.models,
        toolDispatches: answer.toolDispatches,
      };
    },
  "flow.stamped.stamp":
    async (input, context) => ({ output: await toolStamp(input, context) }),
};

/**
 * Every `human:` node this composition declares, by `<flow address>.<node id>`.
 *
 * Read by the hub alone, and for one thing: a pause a **worker** opened settles
 * its dispatch paused (`docs/distributed.md` §3.4), and the hub plants the wait
 * on its own board under the identity the worker derived. What an answer is held
 * to, and what a status route publishes as the contract, are then this node's own
 * — the artifact is everywhere (§4.3), so the descriptor never travels.
 */
runtime.registerHumanNodes({
  "flow.deadline.ask": flowDeadlineNodeAskHuman,
  "flow.escalation.ask": flowEscalationNodeAskHuman,
  "flow.signed_off.approve": flowSignedOffNodeApproveHuman,
});
