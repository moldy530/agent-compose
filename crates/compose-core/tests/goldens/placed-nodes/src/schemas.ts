// This file was generated by agent-compose 0.0.0-dev from `main.yml` (target `mesh`).
//
// It is a build artifact, not source: `agent-compose build` overwrites it,
// and `agent-compose build --check` fails when it has been edited. The spec
// is the single source of truth (PRD 5.12); to own this code instead, copy
// the whole directory out and stop regenerating it.
//
// Every schema the composition declares, lowered per grammar 3.8. These are the
// runtime contract: an agent's output is parsed with the schema named for it
// before any edge is evaluated, a tool's arguments are parsed with its input
// schema, and a state channel's type is the schema its channel is built from.

import { z } from "zod";

/**
 * The length `min_length` and `max_length` bound (grammar 3.4).
 *
 * JSON Schema counts a string's length in Unicode **code points**, and
 * `String.prototype.length` — which `.min()` and `.max()` count — is UTF-16
 * **code units**. Every character outside the BMP is one of the first and two of
 * the second, so the two disagree in both directions on any string carrying one:
 * `"😀"` is one code point and two units. Spreading a string iterates it by code
 * point, which is the rule the schema this composition published states.
 */
const codePoints = (value: string): number => [...value].length;

/** `agent.briefer` — its declared input (grammar 5.3). */
export const agentBrieferInput = z.object({
  path: z.string(),
}).strict();
export type AgentBrieferInput = z.infer<typeof agentBrieferInput>;

/**
 * `agent.briefer` — the structured output the model is constrained to, and what routing reads (PRD 5.2, 5.3).
 */
export const agentBrieferOutput = z.object({
  brief: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type AgentBrieferOutput = z.infer<typeof agentBrieferOutput>;

/** `agent.escalator` — its declared input (grammar 5.3). */
export const agentEscalatorInput = z.object({
  path: z.string(),
}).strict();
export type AgentEscalatorInput = z.infer<typeof agentEscalatorInput>;

/**
 * `agent.escalator` — the structured output the model is constrained to, and what routing reads (PRD 5.2, 5.3).
 */
export const agentEscalatorOutput = z.object({
  approval: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type AgentEscalatorOutput = z.infer<typeof agentEscalatorOutput>;

/** `agent.signer` — its declared input (grammar 5.3). */
export const agentSignerInput = z.object({
  path: z.string(),
}).strict();
export type AgentSignerInput = z.infer<typeof agentSignerInput>;

/**
 * `agent.signer` — the structured output the model is constrained to, and what routing reads (PRD 5.2, 5.3).
 */
export const agentSignerOutput = z.object({
  signature: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type AgentSignerOutput = z.infer<typeof agentSignerOutput>;

/** `flow.batch` — the module's parameters (grammar 7.5). */
export const flowBatchInputs = z.object({
  paths: z.array(z.string()).max(12),
}).strict();
export type FlowBatchInputs = z.infer<typeof flowBatchInputs>;

/**
 * `flow.batch` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowBatchOutputs = z.object({
  signatures: z.array(z.string()).max(12),
}).strict();
export type FlowBatchOutputs = z.infer<typeof flowBatchOutputs>;

/** `flow.conversation` — the module's parameters (grammar 7.5). */
export const flowConversationInputs = z.object({
  path: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type FlowConversationInputs = z.infer<typeof flowConversationInputs>;

/**
 * `flow.conversation` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowConversationOutputs = z.object({
  signature: z.string(),
}).strict();
export type FlowConversationOutputs = z.infer<typeof flowConversationOutputs>;

/** `flow.deadline` — the module's parameters (grammar 7.5). */
export const flowDeadlineInputs = z.object({
  path: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type FlowDeadlineInputs = z.infer<typeof flowDeadlineInputs>;

/**
 * `flow.deadline` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowDeadlineOutputs = z.object({
  approval: z.string(),
}).strict();
export type FlowDeadlineOutputs = z.infer<typeof flowDeadlineOutputs>;

/** `flow.deadline` node `ask` — what the human is shown (grammar 8.7). */
export const flowDeadlineNodeAskInput = z.object({
  path: z.string(),
}).strict();
export type FlowDeadlineNodeAskInput = z.infer<typeof flowDeadlineNodeAskInput>;

/**
 * `flow.deadline` node `ask` — what the human returns, routable like any structured output (grammar 8.7).
 */
export const flowDeadlineNodeAskOutput = z.object({
  decision: z.enum(["approve", "reject"]),
}).strict();
export type FlowDeadlineNodeAskOutput = z.infer<typeof flowDeadlineNodeAskOutput>;

/** `flow.deadline` node `lapse` — what the subprocess produces (grammar 8.2). */
export const flowDeadlineNodeLapseOutput = z.object({
  decision: z.string(),
}).strict();
export type FlowDeadlineNodeLapseOutput = z.infer<typeof flowDeadlineNodeLapseOutput>;

/** `flow.direct` — the module's parameters (grammar 7.5). */
export const flowDirectInputs = z.object({
  path: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type FlowDirectInputs = z.infer<typeof flowDirectInputs>;

/**
 * `flow.direct` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowDirectOutputs = z.object({
  signature: z.string(),
}).strict();
export type FlowDirectOutputs = z.infer<typeof flowDirectOutputs>;

/** `flow.escalated` — the module's parameters (grammar 7.5). */
export const flowEscalatedInputs = z.object({
  path: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type FlowEscalatedInputs = z.infer<typeof flowEscalatedInputs>;

/**
 * `flow.escalated` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowEscalatedOutputs = z.object({
  approval: z.string(),
}).strict();
export type FlowEscalatedOutputs = z.infer<typeof flowEscalatedOutputs>;

/** `flow.escalation` — the module's parameters (grammar 7.5). */
export const flowEscalationInputs = z.object({
  path: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type FlowEscalationInputs = z.infer<typeof flowEscalationInputs>;

/**
 * `flow.escalation` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowEscalationOutputs = z.object({
  approval: z.string(),
}).strict();
export type FlowEscalationOutputs = z.infer<typeof flowEscalationOutputs>;

/** `flow.escalation` node `ask` — what the human is shown (grammar 8.7). */
export const flowEscalationNodeAskInput = z.object({
  path: z.string(),
}).strict();
export type FlowEscalationNodeAskInput = z.infer<typeof flowEscalationNodeAskInput>;

/**
 * `flow.escalation` node `ask` — what the human returns, routable like any structured output (grammar 8.7).
 */
export const flowEscalationNodeAskOutput = z.object({
  decision: z.enum(["approve", "reject"]),
}).strict();
export type FlowEscalationNodeAskOutput = z.infer<typeof flowEscalationNodeAskOutput>;

/** `flow.release` — the module's parameters (grammar 7.5). */
export const flowReleaseInputs = z.object({
  path: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type FlowReleaseInputs = z.infer<typeof flowReleaseInputs>;

/**
 * `flow.release` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowReleaseOutputs = z.object({
  signature: z.string(),
  ticket: z.string(),
}).strict();
export type FlowReleaseOutputs = z.infer<typeof flowReleaseOutputs>;

/** `flow.retried` — the module's parameters (grammar 7.5). */
export const flowRetriedInputs = z.object({
  path: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type FlowRetriedInputs = z.infer<typeof flowRetriedInputs>;

/**
 * `flow.retried` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowRetriedOutputs = z.object({
  signature: z.string(),
}).strict();
export type FlowRetriedOutputs = z.infer<typeof flowRetriedOutputs>;

/** `flow.signed_off` — the module's parameters (grammar 7.5). */
export const flowSignedOffInputs = z.object({
  path: z.string().refine((value) => codePoints(value) >= 1, { message: "expected at least 1 character" }),
}).strict();
export type FlowSignedOffInputs = z.infer<typeof flowSignedOffInputs>;

/**
 * `flow.signed_off` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowSignedOffOutputs = z.object({
  signature: z.string(),
}).strict();
export type FlowSignedOffOutputs = z.infer<typeof flowSignedOffOutputs>;

/** `flow.signed_off` node `approve` — what the human is shown (grammar 8.7). */
export const flowSignedOffNodeApproveInput = z.object({
  signature: z.string(),
}).strict();
export type FlowSignedOffNodeApproveInput = z.infer<typeof flowSignedOffNodeApproveInput>;

/**
 * `flow.signed_off` node `approve` — what the human returns, routable like any structured output (grammar 8.7).
 */
export const flowSignedOffNodeApproveOutput = z.object({
  decision: z.enum(["approve", "reject"]),
}).strict();
export type FlowSignedOffNodeApproveOutput = z.infer<typeof flowSignedOffNodeApproveOutput>;

/** `flow.watched` — the module's parameters (grammar 7.5). */
export const flowWatchedInputs = z.object({
  paths: z.array(z.string()).max(4),
}).strict();
export type FlowWatchedInputs = z.infer<typeof flowWatchedInputs>;

/**
 * `flow.watched` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowWatchedOutputs = z.object({
  signatures: z.array(z.string()).max(12),
}).strict();
export type FlowWatchedOutputs = z.infer<typeof flowWatchedOutputs>;

/** `tool.notarize` — its parameters (grammar 6). */
export const toolNotarizeInput = z.object({
  path: z.string(),
}).strict();
export type ToolNotarizeInput = z.infer<typeof toolNotarizeInput>;

/** `tool.notarize` — its result (grammar 6). */
export const toolNotarizeOutput = z.object({
  ticket: z.string(),
}).strict();
export type ToolNotarizeOutput = z.infer<typeof toolNotarizeOutput>;

/** `tool.sign` — its parameters (grammar 6). */
export const toolSignInput = z.object({
  path: z.string(),
}).strict();
export type ToolSignInput = z.infer<typeof toolSignInput>;

/** `tool.sign` — its result (grammar 6). */
export const toolSignOutput = z.object({
  signature: z.string(),
}).strict();
export type ToolSignOutput = z.infer<typeof toolSignOutput>;

/** State channel `approval` — its declared type (grammar 10.1). */
export const stateApproval = z.string().describe("What the person on the other branch said.").default("");
export type StateApproval = z.infer<typeof stateApproval>;

/** State channel `countersignature` — its declared type (grammar 10.1). */
export const stateCountersignature = z.string().describe("What the second placed branch signed, on its own channel.").default("");
export type StateCountersignature = z.infer<typeof stateCountersignature>;

/** State channel `signature` — its declared type (grammar 10.1). */
export const stateSignature = z.string().describe("What the worker signed, as it came back over the wire.").default("");
export type StateSignature = z.infer<typeof stateSignature>;

/** State channel `signatures` — its declared type (grammar 10.1). */
export const stateSignatures = z.array(z.string()).max(12).describe("One entry per dispatched item, in source-item order.");
export type StateSignatures = z.infer<typeof stateSignatures>;

/** State channel `ticket` — its declared type (grammar 10.1). */
export const stateTicket = z.string().describe("What the hub's own node produced, in the same execution.").default("");
export type StateTicket = z.infer<typeof stateTicket>;
