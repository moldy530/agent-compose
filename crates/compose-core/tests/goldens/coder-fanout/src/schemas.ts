// This file was generated by agent-compose 0.0.0-dev from `main.yml` (target `local`).
//
// It is a build artifact, not source: `agent-compose build` overwrites it,
// and `agent-compose build --check` fails when it has been edited. The spec
// is the single source of truth (PRD 5.12); to own this code instead, copy
// the whole directory out and stop regenerating it.
//
// Every schema the composition declares, lowered per grammar 3.8. These are the
// runtime contract: an agent's output is parsed with the schema named for it
// before any edge is evaluated, a tool's arguments are parsed with its input
// schema, and a state channel's type is the schema its channel is built from.

import { z } from "zod";

/** `flow.batch` — the module's parameters (grammar 7.5). */
export const flowBatchInputs = z.object({
  tasks: z.array(z.object({
    goal: z.string(),
    worktree: z.string(),
  }).strict()).max(4).describe("One entry per checkout, each carrying the directory it is."),
}).strict();
export type FlowBatchInputs = z.infer<typeof flowBatchInputs>;

/**
 * `flow.batch` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowBatchOutputs = z.object({
  summaries: z.array(z.string()).max(4),
  survey: z.string(),
}).strict();
export type FlowBatchOutputs = z.infer<typeof flowBatchOutputs>;

/**
 * `flow.batch` node `summarise` — what the harness answers with, parsed in full by the output gate (grammar 8.9).
 */
export const flowBatchNodeSummariseOutput = z.object({
  note: z.string(),
}).strict();
export type FlowBatchNodeSummariseOutput = z.infer<typeof flowBatchNodeSummariseOutput>;

/** `flow.fix` — the module's parameters (grammar 7.5). */
export const flowFixInputs = z.object({
  goal: z.string(),
  worktree: z.string(),
}).strict();
export type FlowFixInputs = z.infer<typeof flowFixInputs>;

/**
 * `flow.fix` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowFixOutputs = z.object({
  summary: z.string(),
}).strict();
export type FlowFixOutputs = z.infer<typeof flowFixOutputs>;

/**
 * `flow.fix` node `implement` — what the harness run is handed (grammar 8.9).
 */
export const flowFixNodeImplementInput = z.object({
  goal: z.string(),
}).strict();
export type FlowFixNodeImplementInput = z.infer<typeof flowFixNodeImplementInput>;

/**
 * `flow.fix` node `implement` — what the harness answers with, parsed in full by the output gate (grammar 8.9).
 */
export const flowFixNodeImplementOutput = z.object({
  summary: z.string(),
}).strict();
export type FlowFixNodeImplementOutput = z.infer<typeof flowFixNodeImplementOutput>;

/** `flow.review` — the module's parameters (grammar 7.5). */
export const flowReviewInputs = z.object({
  goal: z.string(),
  worktree: z.string(),
}).strict();
export type FlowReviewInputs = z.infer<typeof flowReviewInputs>;

/**
 * `flow.review` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowReviewOutputs = z.object({}).strict();
export type FlowReviewOutputs = z.infer<typeof flowReviewOutputs>;

/**
 * `flow.review` node `inspect` — what the harness run is handed (grammar 8.9).
 */
export const flowReviewNodeInspectInput = z.object({
  goal: z.string(),
}).strict();
export type FlowReviewNodeInspectInput = z.infer<typeof flowReviewNodeInspectInput>;

/**
 * `flow.review` node `inspect` — what the harness answers with, parsed in full by the output gate (grammar 8.9).
 */
export const flowReviewNodeInspectOutput = z.object({
  verdict: z.enum(["approve", "revise"]),
}).strict();
export type FlowReviewNodeInspectOutput = z.infer<typeof flowReviewNodeInspectOutput>;

/** `flow.review_batch` — the module's parameters (grammar 7.5). */
export const flowReviewBatchInputs = z.object({
  tasks: z.array(z.object({
    goal: z.string(),
    worktree: z.string(),
  }).strict()).max(4),
}).strict();
export type FlowReviewBatchInputs = z.infer<typeof flowReviewBatchInputs>;

/**
 * `flow.review_batch` — the module's result, materialized from the state channels of the same names at quiescence (grammar 7.5, 7.6.3).
 */
export const flowReviewBatchOutputs = z.object({}).strict();
export type FlowReviewBatchOutputs = z.infer<typeof flowReviewBatchOutputs>;

/** State channel `summaries` — its declared type (grammar 10.1). */
export const stateSummaries = z.array(z.string()).max(4).describe("One entry per dispatched run, in source-item order.").default([]);
export type StateSummaries = z.infer<typeof stateSummaries>;

/** State channel `summary` — its declared type (grammar 10.1). */
export const stateSummary = z.string().describe("What one implementing run says it changed.").default("");
export type StateSummary = z.infer<typeof stateSummary>;

/** State channel `survey` — its declared type (grammar 10.1). */
export const stateSurvey = z.string().describe("The note the summarising run wrote in its own directory.").default("");
export type StateSurvey = z.infer<typeof stateSurvey>;
